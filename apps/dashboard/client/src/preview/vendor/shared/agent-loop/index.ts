// PREVIEW (UI mock, fake data): delete when the one-product dashboard lands. See docs/ONE_PRODUCT_PLAN.md §3.5.
// The agentic branch's agent-loop session contract, copied for the vendored lib/api.
// This branch's dashboard client has no @truecourse/agent-loop dependency; the preview
// carries the two type modules its vendored API client names.

export * from './session-events';
export * from './session-store';
