/**
 * The mono code-block class every guard detail pane shares (expected/actual,
 * YAML source, evidence, program output). One definition — the five detail
 * components carried verbatim copies before it was hoisted here.
 */
export const PRE =
  'mt-1 max-h-72 overflow-auto whitespace-pre-wrap break-all rounded border border-border bg-muted/20 p-2 font-mono text-[11px] text-foreground';
