/**
 * The open edition's edition module: there is nothing to add.
 *
 * `main.tsx` imports this through the `@edition` alias, which the build points
 * at the enterprise bundle instead when the checkout has one.
 */

export function registerEditionFeatures(): void {}
