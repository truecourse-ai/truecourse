// Fixture Next config — a plain one, so the route manifest reads this app as
// declaring exactly what its route files say. (The forwarding case is exercised
// by a test that writes its own config into a temp repo.)
module.exports = { reactStrictMode: true }
