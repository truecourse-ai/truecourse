field-exposure id.exposure {
  // inferred — code exposes `id` on a read path (query-select + api-response) but no spec records this exposure
  inferred-from "app/services/customers_profile.py" 29..29
  confidence high
  field id
  via query-select
  via api-response
}
