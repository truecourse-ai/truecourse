architecture-decision persistence.beta_features {
  // inferred — field `beta_features` is persisted as metadata-json (read from a JSON metadata blob (no dedicated column)) but no ADR records this storage choice
  inferred-from "app/services/customers_preferences.py" 37..37
  confidence medium
  category persistence-strategy
  chosen metadata-json
  reason "field `beta_features` is persisted as metadata-json (read from a JSON metadata blob (no dedicated column)) but no ADR records this storage choice"
}
