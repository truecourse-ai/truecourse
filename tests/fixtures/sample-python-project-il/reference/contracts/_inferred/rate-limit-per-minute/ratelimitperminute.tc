constant RATE_LIMIT_PER_MINUTE {
  // inferred — policy constant defined in code but documented in no spec
  inferred-from "app/rate_limit.py" 10..10
  confidence high
  type number
  expected-value 100
}
