// Link to the canonical public API documentation — correctly hardcoded as the docs host doesn't vary.
function ApiTokenSettings() {
  return (
    <section>
      <p>
        Generate API tokens to use with the <a href="https://docs.myapp.io/api-reference" target="_blank" rel="noreferrer">MyApp Public API</a>.
      </p>
    </section>
  );
}
