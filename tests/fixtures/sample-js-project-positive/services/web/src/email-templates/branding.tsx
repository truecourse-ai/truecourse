/**
 * Re-export of an imported identifier. The file just re-exports the
 * default from another module. There is no class declared here whose
 * name should match the filename — the rule should NOT fire.
 *
 * Mirrors documenso `apps/remix/app/routes/.../branding.tsx`.
 */

import ConfirmEmailTemplate from './confirm-email';

export default ConfirmEmailTemplate;
