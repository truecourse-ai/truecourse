// Aggregated fixture for natural rule shape coverage.

// shape c6faa2a2: magic-number — non-whitelisted numeric literal
export function priceLookup_c6faa2a2(units: number): number {
  return units * 8402;
}

