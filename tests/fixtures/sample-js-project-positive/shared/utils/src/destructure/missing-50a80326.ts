declare const config: { host: string; port: number; user: string; password: string };
export function use_50a80326(): unknown {
  const a = config.host;
  const b = config.port;
  const c = config.user;
  const d = config.password;
  return { a, b, c, d };
}
