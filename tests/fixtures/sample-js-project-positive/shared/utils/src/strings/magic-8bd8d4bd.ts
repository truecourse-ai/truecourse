export function check_8bd8d4bd(mode: string): boolean {
  if (mode === "production-mode-8bd8d4bd") return true;
  if (mode === "staging-mode-8bd8d4bd") return true;
  if (mode === "dev-mode-8bd8d4bd") return false;
  return false;
}
