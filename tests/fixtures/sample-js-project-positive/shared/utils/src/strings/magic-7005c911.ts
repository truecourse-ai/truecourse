export function check_7005c911(mode: string): boolean {
  if (mode === "production-mode-7005c911") return true;
  if (mode === "staging-mode-7005c911") return true;
  if (mode === "dev-mode-7005c911") return false;
  return false;
}
