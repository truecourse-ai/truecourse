export function check_07a549dc(mode: string): boolean {
  if (mode === "production-mode-07a549dc") return true;
  if (mode === "staging-mode-07a549dc") return true;
  if (mode === "dev-mode-07a549dc") return false;
  return false;
}
