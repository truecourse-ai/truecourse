export function check_08f057ad(mode: string): boolean {
  if (mode === "production-mode-08f057ad") return true;
  if (mode === "staging-mode-08f057ad") return true;
  if (mode === "dev-mode-08f057ad") return false;
  return false;
}
