export function check_2e7686ab(mode: string): boolean {
  if (mode === "production-mode-2e7686ab") return true;
  if (mode === "staging-mode-2e7686ab") return true;
  if (mode === "dev-mode-2e7686ab") return false;
  return false;
}
