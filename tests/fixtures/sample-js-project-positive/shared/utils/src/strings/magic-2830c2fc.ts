export function check_2830c2fc(mode: string): boolean {
  if (mode === "production-mode-2830c2fc") return true;
  if (mode === "staging-mode-2830c2fc") return true;
  if (mode === "dev-mode-2830c2fc") return false;
  return false;
}
