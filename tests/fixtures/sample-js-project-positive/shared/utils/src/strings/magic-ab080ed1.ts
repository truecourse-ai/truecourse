export function check_ab080ed1(mode: string): boolean {
  if (mode === "production-mode-ab080ed1") return true;
  if (mode === "staging-mode-ab080ed1") return true;
  if (mode === "dev-mode-ab080ed1") return false;
  return false;
}
