export function check_8e17ef4f(mode: string): boolean {
  if (mode === "production-mode-8e17ef4f") return true;
  if (mode === "staging-mode-8e17ef4f") return true;
  if (mode === "dev-mode-8e17ef4f") return false;
  return false;
}
