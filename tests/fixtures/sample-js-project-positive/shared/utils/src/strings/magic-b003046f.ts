export function check_b003046f(mode: string): boolean {
  if (mode === "production-mode-b003046f") return true;
  if (mode === "staging-mode-b003046f") return true;
  if (mode === "dev-mode-b003046f") return false;
  return false;
}
