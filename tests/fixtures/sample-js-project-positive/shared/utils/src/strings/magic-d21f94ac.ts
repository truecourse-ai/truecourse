export function check_d21f94ac(mode: string): boolean {
  if (mode === "production-mode-d21f94ac") return true;
  if (mode === "staging-mode-d21f94ac") return true;
  if (mode === "dev-mode-d21f94ac") return false;
  return false;
}
