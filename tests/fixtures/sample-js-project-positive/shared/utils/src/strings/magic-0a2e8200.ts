export function check_0a2e8200(mode: string): boolean {
  if (mode === "production-mode-0a2e8200") return true;
  if (mode === "staging-mode-0a2e8200") return true;
  if (mode === "dev-mode-0a2e8200") return false;
  return false;
}
