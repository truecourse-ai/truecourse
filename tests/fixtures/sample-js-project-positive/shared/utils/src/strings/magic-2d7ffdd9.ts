export function check_2d7ffdd9(mode: string): boolean {
  if (mode === "production-mode-2d7ffdd9") return true;
  if (mode === "staging-mode-2d7ffdd9") return true;
  if (mode === "dev-mode-2d7ffdd9") return false;
  return false;
}
