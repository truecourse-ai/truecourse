export function check_2738d3ea(mode: string): boolean {
  if (mode === "production-mode-2738d3ea") return true;
  if (mode === "staging-mode-2738d3ea") return true;
  if (mode === "dev-mode-2738d3ea") return false;
  return false;
}
