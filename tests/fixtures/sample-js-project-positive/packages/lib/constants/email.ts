// Token type identifier for DB lookup — identifies the purpose of a verification token, not a secret value
export const EMAIL_CONFIRMATION_TOKEN_TYPE = 'confirmation-email';

export const EMAIL_CHANGE_TOKEN_TYPE = 'email-change';
