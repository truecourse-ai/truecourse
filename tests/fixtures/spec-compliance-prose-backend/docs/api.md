The backend must expose a ping endpoint at GET /ping.

The backend must expose a hello endpoint at POST /hello. The hello endpoint must accept a JSON body with a name field and use that name to return a greeting in the form "hello <user>".
