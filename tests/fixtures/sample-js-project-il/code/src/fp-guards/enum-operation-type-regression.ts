// Regression: PluginEventType has no code counterpart — no sibling-module id-literal
// pattern exists for these values anywhere in the codebase.
// no-code-counterpart must still fire after the sibling synthesis fix.
// IL-DRIFT: Enum:PluginEventType / enum.PluginEventType.no-code-counterpart
