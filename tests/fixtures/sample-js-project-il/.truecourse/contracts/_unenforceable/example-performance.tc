// Hypothetical example — the live SPEC.md doesn't yet contain a
// Performance section. Kept here to show the artifact shape.

unenforceable-obligation performance.api.p99 {
  origin SPEC.md "Performance (hypothetical)" -1..-1

  spec-text "API endpoints under /api/* must have p99 latency under 200ms at 1000 RPS."
  category performance

  rationale "Requires runtime measurement under load. No static structural artifact captures this. A future PerformanceSLA artifact would need observability integration (Prometheus, k6, etc.) we don't currently model."

  could-become-enforceable-via PerformanceSLA
}
