query-rule loyalty_tiers.eq-is_active {
  // inferred — every query against `loyalty_tiers` constrains `is_active` but no spec records this data policy
  inferred-from "src/repos/loyalty.repo.ts" 34..37
  confidence high
  entity Entity:loyalty_tiers
  required {
    eq is_active true
  }
}
