# Semantic Match Results: ESLint

## COVERED (already in ALL-RULES.md)
| ESLint Rule | Description | Matched Our Key | Reason |
|-------------|-------------|-----------------|--------|
| no-undef | Disallow the use of undeclared variables unless mentioned in `/*global */` comments | code-quality/deterministic/implicit-global | SonarJS S2703 covers undeclared/implicit globals in JS/TS; also partially covered by bugs/deterministic/unassigned-variable |
| default-case | Require `default` cases in `switch` statements | code-quality/deterministic/elseif-without-else | Related concept: missing default branch. Also covered by SonarJS S4524 (default-case-last) which implies a default case exists |
| max-depth | Enforce a maximum depth that blocks can be nested | code-quality/deterministic/deeply-nested-logic | SonarJS S134 (nested-control-flow) covers max nesting depth |
| max-statements | Enforce a maximum number of statements allowed in function blocks | code-quality/deterministic/long-method | SonarJS S138 (max-lines-per-function) and truecourse long-method cover function size limits |
| no-extra-label | Disallow unnecessary labels | code-quality/deterministic/labels-usage | Labels usage (S1119) covers labels broadly; extra labels are a subset |
| no-unused-labels | Disallow unused labels | code-quality/deterministic/labels-usage | Labels usage (S1119/no-labels) covers all label-related issues |
| no-implicit-globals | Disallow declarations in the global scope | architecture/deterministic/declarations-in-global-scope | SonarJS S3798 (declarations-in-global-scope) is the exact same concept |
| no-invalid-this | Disallow use of `this` in contexts where the value of `this` is `undefined` | bugs/deterministic/global-this-usage | SonarJS S2990 (no-global-this) covers misuse of this outside proper context |
| no-invalid-this (@typescript-eslint) | Disallow `this` keywords outside of classes or class-like objects | bugs/deterministic/global-this-usage | Same as above, TypeScript variant |
| no-restricted-imports | Disallow specified modules when loaded by `import` | (project-specific, not a general rule) | This is a configuration-driven rule; the concept of restricting imports is not a universal code quality rule |
| no-restricted-imports (@typescript-eslint) | Disallow specified modules when loaded by `import` | (project-specific, not a general rule) | Duplicate of ESLint core version for TypeScript |
| no-extra-non-null-assertion | Disallow extra non-null assertions | bugs/deterministic/contradictory-non-null-coalescing + code-quality/deterministic/non-null-assertion | Extra !! non-null assertions covered by the combination of non-null assertion rules |
| no-inferrable-types | Disallow explicit type declarations for variables initialized to a number, string, or boolean | code-quality/deterministic/unnecessary-type-assertion | Conceptually similar: unnecessary/redundant type annotations. Also related to unnecessary-type-conversion |
| init-declarations | Require or disallow initialization in variable declarations | code-quality/deterministic/undefined-assignment | Partially overlaps: assigning undefined vs not initializing |
| init-declarations (@typescript-eslint) | Require or disallow initialization in variable declarations | code-quality/deterministic/undefined-assignment | Duplicate of ESLint core version |
| no-warning-comments | Disallow specified warning terms in comments | code-quality/deterministic/todo-fixme | S1135/S1134 (todo-tag/fixme-tag) covers TODO/FIXME/HACK warning comments |
| no-regex-spaces | Disallow multiple spaces in regular expressions | code-quality/deterministic/regex-multiple-spaces | SonarJS S6326 (no-regex-spaces) is the exact same rule |
| no-eq-null | Disallow `null` comparisons without type-checking operators | code-quality/deterministic/strict-equality | Covered by eqeqeq (strict equality) which catches == null vs === null |

## SKIP (excluded category)
| ESLint Rule | Description | Reason |
|-------------|-------------|--------|
| arrow-body-style | Require braces around arrow function bodies | Stylistic: brace style preference for arrow functions |
| camelcase | Enforce camelcase naming convention | Naming convention |
| capitalized-comments | Enforce or disallow capitalization of the first letter of a comment | Formatting/stylistic: comment capitalization |
| consistent-this | Enforce consistent naming when capturing the current execution context | Naming convention (this aliasing name) |
| curly | Enforce consistent brace style for all control statements | Formatting/stylistic: brace style |
| dot-notation | Enforce dot notation whenever possible | Stylistic: property access style preference |
| func-name-matching | Require function names to match the name of the variable or property to which they are assigned | Naming convention |
| func-names | Require or disallow named `function` expressions | Naming convention / stylistic |
| func-style | Enforce the consistent use of either `function` declarations or expressions | Stylistic: function declaration style preference |
| id-denylist | Disallow specified identifiers | Naming convention (project-specific identifier blocklist) |
| id-length | Enforce minimum and maximum identifier lengths | Naming convention |
| id-match | Require identifiers to match a specified regular expression | Naming convention |
| logical-assignment-operators | Require or disallow logical assignment operator shorthand | Stylistic: operator shorthand preference |
| new-cap | Require constructor names to begin with a capital letter | Naming convention |
| no-bitwise | Disallow bitwise operators | Stylistic: operator restriction (bitwise in boolean context is already covered separately) |
| no-continue | Disallow `continue` statements | Stylistic: control flow preference |
| no-div-regex | Disallow equal signs explicitly at the beginning of regular expressions | Formatting/stylistic: regex readability |
| no-inline-comments | Disallow inline comments after code | Formatting/stylistic: comment placement |
| no-multi-str | Disallow multiline strings | Stylistic: string style preference |
| no-plusplus | Disallow the unary operators `++` and `--` | Stylistic: operator preference |
| no-ternary | Disallow ternary operators | Stylistic: control flow preference (overly restrictive) |
| no-undef-init | Disallow initializing variables to `undefined` | Stylistic: initialization preference |
| no-undefined | Disallow the use of `undefined` as an identifier | Stylistic: undefined usage preference |
| no-underscore-dangle | Disallow dangling underscores in identifiers | Naming convention |
| object-shorthand | Require or disallow method and property shorthand syntax for object literals | Stylistic: shorthand preference |
| one-var | Enforce variables to be declared either together or separately in functions | Formatting/stylistic: declaration grouping |
| operator-assignment | Require or disallow assignment operator shorthand where possible | Stylistic: operator shorthand preference |
| prefer-arrow-callback | Require using arrow functions for callbacks | Stylistic: function style preference |
| prefer-exponentiation-operator | Disallow the use of `Math.pow` in favor of the `**` operator | Stylistic: operator preference |
| prefer-numeric-literals | Disallow `parseInt()` and `Number.parseInt()` in favor of binary, octal, and hexadecimal literals | Stylistic: numeric literal preference |
| require-unicode-regexp | Enforce the use of `u` or `v` flag on RegExp | Stylistic: regex flag preference (unicode-aware-regex S5867 is already in catalog as code-quality rule) |
| sort-imports | Enforce sorted import declarations within modules | Formatting/stylistic: import ordering |
| sort-keys | Require object keys to be sorted | Formatting/stylistic: key ordering |
| sort-vars | Require variables within the same declaration block to be sorted | Formatting/stylistic: variable ordering |
| strict | Require or disallow strict mode directives | Stylistic/legacy: strict mode directive (irrelevant with ES modules) |
| symbol-description | Require symbol descriptions | Stylistic: Symbol() description requirement |
| vars-on-top | Require `var` declarations be placed at the top of their containing scope | Formatting/stylistic: declaration placement (var itself is already flagged by no-var) |
| yoda | Require or disallow "Yoda" conditions | Stylistic: condition ordering (Python version is in catalog but JS version is stylistic preference) |
| adjacent-overload-signatures | Require that function overload signatures be consecutive | Formatting/stylistic: declaration ordering |
| array-type | Require consistently using either `T[]` or `Array<T>` for arrays | Stylistic: type syntax preference |
| ban-tslint-comment | Disallow `// tslint:<rule-flag>` comments | Deprecated tooling cleanup (tslint is dead) |
| class-literal-property-style | Enforce that literals on classes are exposed in a consistent style | Stylistic: class property style preference |
| consistent-generic-constructors | Enforce specifying generic type arguments on type annotation or constructor name | Stylistic: generic placement preference |
| consistent-indexed-object-style | Require or disallow the `Record` type | Stylistic: indexed object syntax preference |
| consistent-type-assertions | Enforce consistent usage of type assertions | Stylistic: type assertion syntax preference (as vs angle bracket) |
| consistent-type-definitions | Enforce type definitions to consistently use either `interface` or `type` | Stylistic: type definition preference |
| explicit-member-accessibility | Require explicit accessibility modifiers on class properties and methods | Stylistic: access modifier preference |
| member-ordering | Require a consistent member declaration order | Formatting/stylistic: member ordering |
| method-signature-style | Enforce using a particular method signature syntax | Stylistic: signature syntax preference |
| no-restricted-types | Disallow certain types | Project-specific configuration rule (type blocklist) |
| parameter-properties | Require or disallow parameter properties in class constructors | Stylistic: constructor parameter property preference |
| prefer-namespace-keyword | Require using `namespace` keyword over `module` keyword | Stylistic: keyword preference (namespace usage is already flagged) |
| typedef | Require type annotations in certain places | Stylistic: type annotation enforcement (missing-return-type and missing-boundary-types cover API boundaries already) |
| no-restricted-exports | Disallow specified names in exports | Project-specific configuration rule |
| no-restricted-globals | Disallow specified global variables | Project-specific configuration rule |
| no-restricted-properties | Disallow certain properties on certain objects | Project-specific configuration rule |
| no-restricted-syntax | Disallow specified syntax | Project-specific configuration rule |

## MISSING (genuinely not covered)
| ESLint Rule | Description | Suggested Domain |
|-------------|-------------|-----------------|
| (none) | | |

All unmatched ESLint rules are either already covered semantically in ALL-RULES.md under different names, are stylistic/formatting/naming convention rules that should be excluded, or are project-specific configuration rules (no-restricted-*) that are not universal code quality rules.
