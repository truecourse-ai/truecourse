# Live re-verification brief (plan step 3)

You are re-verifying findings from `docs/findings/targets/<target>/report.md` against a FRESH instance of the product built from today's default branch. The original guard run's seed scripts are not on this machine, so this is a manual reproduction: build the product, seed what the scenarios need by hand, replay the scenario's requests (curl / a small node script for API scenarios, playwright-core for web scenarios), capture request + response as evidence, compare with the original transcript, and record a verdict per finding. You file nothing upstream. You do not touch any live production service.

All paths are absolute or relative to `/Users/musheghgevorgyan/repos/truecourse/docs/findings/targets/`.

## Ground rules

- Work dir for the build: `/private/tmp/claude-501/-Users-musheghgevorgyan-repos-truecourse/ace1ded0-15bd-489a-81e7-579caf056682/scratchpad/build/<repo>`. Clone fresh and shallow from GitHub (`git clone --depth 1 --branch <default> https://github.com/<owner>/<repo>.git`), record `git rev-parse HEAD` and the commit date, and confirm the sha matches `targets/STATE.md` (if upstream moved since, that is fine, record the new sha and tell me).
- **Disk is scarce: about 10 GB free on this machine.** Run `df -h /System/Volumes/Data` before installing and after building. If free space drops under 2 GB, stop and report rather than filling the disk. When you are completely done (evidence written, report written), delete the build dir's `node_modules`, build outputs and the clone itself (`rm -rf .../build/<repo>`), and leave the database you created (it is tiny) but note its name. Never delete anything outside your build dir, the databases you created, and your own temp files.
- Do NOT start, stop or restart any service you did not start yourself (the user's `pnpm dev`, Docker, the homebrew Postgres/Redis). Postgres 17 is already running on 127.0.0.1:5432 (superuser `postgres`; try `psql -h 127.0.0.1 -U postgres -c 'select 1'`; if it needs a password check `~/.pgpass` or ask for a role you can create a database with, and if you cannot connect at all, report and stop rather than installing another Postgres). Redis is running on 127.0.0.1:6379. Docker is NOT running; do not start it. Create your own databases with distinct names (e.g. `tc_reverify_documenso`, `tc_reverify_caldiy`) so nothing else is touched.
- Use ports nobody else uses (Strapi 1347, Documenso 3347, Cal.diy API 5347 / web 3348). Check with `lsof -i :<port>` first.
- Never run more than one heavy build at a time (8 cores, 16 GB RAM).
- Never edit the guard stores under `/Users/musheghgevorgyan/repos/*/.truecourse/` and never edit the review JSONs. Your outputs go under `filing/live/<repo>/` only.
- For web scenarios use `playwright-core` 1.62.1, loadable from `/Users/musheghgevorgyan/repos/truecourse/packages/guard-runner` (`cd` there, or `NODE_PATH=/Users/musheghgevorgyan/repos/truecourse/packages/guard-runner/node_modules`), chromium is installed. Prefer headless; take screenshots at each assertion; use the accessibility snapshot (`page.accessibility.snapshot()` or `locator.ariaSnapshot()`) for accessible-name claims (C11, D8, D9).
- No em dashes anywhere in your output. Plain language.
- Do not guess. If a step cannot be reproduced, say why (build failed, feature needs data you could not seed, etc.) and mark it `could not reproduce` with the reason. Never report a verdict you did not observe.

## Inputs

- `targets/FINDINGS-INDEX.json`: finding id -> scenario ids, review JSON, culprit files, `liveMinimum` (the set you MUST attempt), `liveNote` (extra things to exercise).
- `<repo>/<scenario-id>.json` (review JSON): what failed, the doc claim, `observed`, root cause, notes (often contains a reproduction recipe for maintainers).
- Guard store for the repo: `/Users/musheghgevorgyan/repos/<strapi|documenso|cal.diy>/.truecourse/`:
  - `scenarios/<area>/<scenario-id>.yaml`: the exact steps (method, path, headers, json body, expect). `{{fixture:...}}` / `{{cred:...}}` are seed-provided values: substitute your own. `${name}` are captures from earlier steps. `${unique}` is a per-run random suffix.
  - `guard/LATEST.json` -> `scenarios[].evidencePath` gives the evidence dir; `transcript.txt` inside is the original step-by-step log with requests, responses and verdicts. Read it before reproducing: it tells you what the product did and the bodies that were sent. `invocation.json` is the failing step's request, `response.txt` the failing response. Web scenarios have `step-N.png` screenshots.
  - `scenarios/recipe.json`: how the original run built and served the product (install, build, serve, env). Follow it unless the section below says otherwise.
- `targets/STATE.md`: heads and tags as of today.

## Per finding, record

`filing/live/<repo>/<ID>/` (ID as in FINDINGS-INDEX, e.g. `S1`, `D2a`) containing:
- `repro.md`: date, build (sha, tag if any, how started), the numbered steps you ran with the exact requests (method, url, headers with secrets redacted, body) and the exact responses (status, relevant headers, body, truncated to what matters with a note), the comparison with the original transcript, and the verdict.
- Raw captures: `step-N.request.json` / `step-N.response.json` (or `.txt`), screenshots for web, any server log excerpt that matters (`server.log` excerpt with timestamps).
- Verdict vocabulary: `still reproduces` | `fixed` (name what changed if you can see it) | `changed` (behavior differs from the original but the defect is not fixed; describe) | `could not reproduce` (with the reason: could not seed, build broke, timed out, behavior depends on state you could not create).

Then one summary `filing/live/<repo>/REPORT.md`: a table (id, verdict, one-line evidence, evidence path) and a short section on how the instance was built and seeded (commands, env, what you created), problems hit, and anything that should change a finding's confidence or scope. Also write `filing/live/<repo>/summary.json`: `{ "repo": "...", "build": {"sha": "", "date": "", "tag": null}, "checkedAt": "2026-08-19", "findings": { "<ID>": {"verdict": "...", "evidence": "relative path", "note": "..."} } }`.

When done, reply with at most 10 plain lines: per finding id the verdict, plus build sha and the disk state. Nothing else.

---

## Strapi (repo `strapi`, default branch `develop`)

- Build: clone `strapi/strapi` `develop` shallow. Node 24 is installed (engines allow <=26). Yarn 4.12.0 via `corepack enable` (or `corepack yarn`). `yarn install`, then `yarn build` (monorepo; long), then `yarn workspace getstarted build`. If `yarn build` fails on a package unrelated to the server (docs, e2e), try building only what the getstarted app needs (`yarn build --filter ...` / check `package.json` scripts) and say so. Record build time.
- Run `examples/getstarted` (it already contains the `api::article.article` and `api::category.category` content types the scenarios use, localized, draftAndPublish, with `mcp.enabled: true` and sqlite at `.tmp/data.db`; `config/api.js` pins `maxLimit: 30`, which is the S17 environment artifact, leave it). Start with `PORT=1347 STRAPI_TELEMETRY_DISABLED=true yarn workspace getstarted start` (or `develop`), log to a file. Health: `GET /_health` -> 204.
- Seed by hand: `POST /admin/register-admin` for the first super admin (body `{email, password, firstname, lastname}`; password policy needs upper/lower/digit, 8+ chars). Login `POST /admin/login` -> `data.token`. Content-API tokens: `POST /admin/api-tokens` `{name, type: "full-access", lifespan: null}` and `{type: "read-only"}` -> `data.accessKey`. Admin token for MCP: `POST /admin/admin-tokens` with `adminPermissions` as in the scenario yaml (S1 uses exactly `[{action: "plugin::content-manager.explorer.read", subject: "api::article.article"}]`; the relation/filter/list scenarios need create/read/update/delete on article and category: read their yaml setup steps). Create a category and a few articles through `POST /api/categories` / `POST /api/articles` with the full-access token (or through MCP). The MCP endpoint is `POST /mcp` with `Authorization: Bearer <admin token accessKey>`, `Accept: application/json, text/event-stream`, JSON-RPC bodies `{jsonrpc:"2.0", id, method:"tools/list"|"tools/call", params}`; responses may be SSE (`text/event-stream`): parse the `data:` lines.
- Minimum set (all `liveMinimum: true` in the index): S1 (grant, confirm tools/list, restart the server, tools/list again; also `GET /admin/admin-tokens/:id` after restart to show `adminPermissions` empty), S2/S3/S4 (the relation scenario: `set`+`connect` in one `update_article` call, `set: null`, and the `categories: []` in every create/update reply while `get_article` shows the truth; the S3 step ran in the sibling run `2026-08-14T15-15-28Z_32215874` of the same scenario, read that transcript too), S5 (`$null: true` / `$notNull: true` on `authorName` rejected; `$null: null` accepted and behaving as `whereNotNull`), S6 (a write error echoing raw SQL: easiest is to hold a write lock on the sqlite file from another process, e.g. `sqlite3 .tmp/data.db 'BEGIN IMMEDIATE; SELECT 1;'` kept open via a fifo/`sleep`, or run ~20 concurrent `create_article` calls; the original hit `database is locked`; capture the MCP error text verbatim and show whether it contains the SQL with bound values; also try the same failure through REST `POST /api/articles` to show REST hides it), S7 (`list_article` with no pageSize -> `pagination.pageSize` 10 vs the tool's advertised schema default 25 in `tools/list`; REST `GET /api/articles` -> 25), S8 (`DELETE /api/articles/:documentId?status=draft` -> 500, and `?status=published` on a published doc -> 204 and the draft row gone), S9 (`DELETE /api/articl/<id>` or any unrouted path with a non-GET verb -> 405 `Allow: HEAD, GET`, text body; `GET` same path -> JSON 404), S10 (web: log into `/admin`, Content Manager -> Article list, select published entries, Unpublish bulk action, read the confirm dialog's button label; screenshot; compare with the bulk Publish dialog).
- Also attempt (cheap, not in the minimum set): S11's escalation: create a second admin user with a role that holds only `admin::admin-tokens.read` (or give an admin token only `admin::admin-tokens.read` if tokens can hold that action), then `GET /admin/admin-tokens/:otherId` and see whether `accessKey` of a token owned by someone else comes back in plaintext; also confirm `GET /admin/admin-tokens/:id` returns the owner's own `accessKey` on every read. Record request/response. S16 (`GET /api/articles/:documentId` has no per-entry `meta`), S15 (`GET /api/articles/<unknown>` message text; write with the read-only token -> 403 body message). These are one curl each.
- Record the Strapi version the instance reports (`GET /admin/init` or `package.json` of `@strapi/strapi`).

## Documenso (repo `documenso`, default branch `main`, which today equals tag `v2.17.0`)

- Build: clone `documenso/documenso` `main` shallow (confirm `git describe --tags` / the sha equals v2.17.0 = 75330166cc). Node >= 22 (24 is installed), npm 11.11.0 (`npx --yes npm@11.11.0 ci` as the recipe does, or `corepack`). Build `npx turbo run build --filter=@documenso/remix`. Postgres: create database `tc_reverify_documenso` on the local 5432 instance with a role/password you create (or use the `postgres` superuser if it has no password); set `NEXT_PRIVATE_DATABASE_URL` and `NEXT_PRIVATE_DIRECT_DATABASE_URL` accordingly; `npm run prisma:generate && npm run prisma:migrate-deploy`. Env from `scenarios/recipe.json` (`NEXTAUTH_SECRET`, encryption keys, `NEXT_PUBLIC_UPLOAD_TRANSPORT=database`, `NEXT_PRIVATE_JOBS_PROVIDER=local`, `NEXT_PRIVATE_SIGNING_TRANSPORT=local`, `NEXT_PRIVATE_SIGNING_LOCAL_FILE_PATH=./example/cert.p12`, SMTP: there is no mail catcher running; start a tiny SMTP sink yourself if the app insists on one (a 20-line node `net` server that speaks enough SMTP to accept a message, or `NEXT_PRIVATE_SMTP_TRANSPORT` set to a transport that does not send, check `packages/lib/server-only/email/` for what exists) and say which; `DOCUMENSO_DISABLE_TELEMETRY=true`). For D6 do NOT set `DANGEROUS_BYPASS_RATE_LIMITS` (the original run did set it and still saw `x-ratelimit-limit: 100`; record the headers both ways if cheap). `NEXT_PUBLIC_WEBAPP_URL=http://localhost:3347`, `PORT=3347`. Start `npm run start -w @documenso/remix`. Health `/api/health`.
- Seed by hand: use the repo's own seed helpers, `packages/prisma/seed/users.ts` (`seedUser`), `teams.ts`, and `packages/lib/server-only/public-api/create-api-token.ts` (`createApiToken`) from a `tsx` script run inside the repo (the test suite does the same), or the signup UI plus Settings -> API tokens. You need: an owner user + team with an API token, an outsider user with its own token, and a small PDF (`example/` or `assets/` in the repo has sample PDFs, `packages/prisma/seed/documents.ts` shows how a PDF is attached). The v2 API is under `/api/v2/...` with `Authorization: <token>` (no `Bearer`, the scenario headers show it).
- Minimum set: D1 (create envelope, add recipient + signature field, distribute/send, then `POST /api/v2/envelope/recipient/create` on the PENDING envelope -> 200 vs the documented refusal; and the never-run steps 8-12 of the yaml: recipient update, recipient delete, field create, field update, field delete on the sent envelope, each recorded), D2a + D2b (`POST /api/v2/envelope/field/update-many` with a new positionY: read the field back, show the coordinate unchanged (D2a) and the fieldMeta reset to defaults (D2b); read the yaml + transcript for the exact bodies), D3 (ASSISTANT recipient on an envelope with `signingOrder: PARALLEL` -> accepted), D4 (`.../reject` on behalf of a recipient -> 200, then immediately `GET` the envelope: status still PENDING; poll a few times at 0, 0.5, 2, 5 s and record when/if it flips), D5 (field with `fieldMeta.required: true, readOnly: true` -> 200), D6 (any v2 call: `x-ratelimit-limit` header value and whether the headers are present; expected 1000 on v2.17.0), D7 (web: sign in as owner, create a document, add a recipient, send with distribution method None, land on the document page, record whether any `/sign/` link is displayed; screenshot; also record which `?action=` value the redirect carries), D8 (web: Settings -> API tokens -> the expiry `Select`: accessibility snapshot showing the combobox's accessible name), D9 (web: on the document page after the None send, the copy-link button's and the three-dots trigger's accessible names).
- Also, cheap: D15 (`GET /api/v2/envelope/<owner envelope>` with the outsider token -> 404 not 403), D10 (the error envelope shape on an unknown envelope id, and on a zod 400), D13/D14 (`POST /api/v2/envelope/get-many` with `{envelopeIds}` -> 400; the list response's pagination keys), D11 (coordinates as strings in the create response). One call each.

## Cal.diy (repo `cal.diy`, default branch `main` @ 176037d0af, unchanged since 2026-08-08)

- This is the heaviest. Clone `calcom/cal.diy` `main` shallow. `yarn` (yarn 3/4 via corepack; check `packageManager`). Postgres database `tc_reverify_caldiy` on local 5432; Redis at `redis://localhost:6379`. Env from `scenarios/recipe.json` (`DATABASE_URL`, `DATABASE_DIRECT_URL` pointing at your db, `NEXTAUTH_SECRET`, `CALENDSO_ENCRYPTION_KEY`, `CALCOM_SERVICE_ACCOUNT_ENCRYPTION_KEY`, `API_KEY_PREFIX=cal_`, `EMAIL_SERVER_*` pointing at a port with nothing on it is fine if sending just fails asynchronously; check `.env.example`), `NEXT_PUBLIC_WEBAPP_URL=http://localhost:3348`, `NEXTAUTH_URL=http://localhost:3348`. `yarn workspace @calcom/prisma prisma generate`, `yarn workspace @calcom/prisma db-deploy` (migrations).
- API v2: `yarn workspace @calcom/api-v2 dev:build && yarn workspace @calcom/api-v2 build`, then `PORT=5347 node apps/api/v2/dist/apps/api/v2/src/main.js` (check `apps/api/v2/.env.example` for the variables the API needs, e.g. `API_PORT`, `NEXTAUTH_SECRET`, `DATABASE_*`, `REDIS_URL`, `JWT_SECRET`; the health path is `/health`). Requests carry `cal-api-version: 2024-08-13` (bookings) / `2024-09-04` (slots) as the scenarios do; the docs' `2026-02-25` does not exist in Cal.diy, that is a known edition mismatch.
- Web (needed for C1, C10, C11, C12, C13): a production `next build` of `apps/web` is very heavy on this machine; prefer `yarn workspace @calcom/web dev` on port 3348 (pages compile on first hit, allow 60-120 s per first page load) and say so in the report. If dev mode breaks a scenario (hydration timing), say so.
- Seed by hand: the repo's `yarn workspace @calcom/prisma db-seed` creates demo users and event types (read `packages/prisma/seed.ts` to see what, e.g. user `pro` with seated and requires-confirmation event types); then add what the scenarios need with a small Prisma script (`packages/prisma` client) or the web UI: a host user with an API key (ApiKey rows store `hashedKey`; see `packages/features/ee/api-keys/lib/apiKeys.ts` `hashAPIKey` / `generateUniqueAPIKey`), a 30 min event type, a seated event type (`seatsPerTimeSlot`), a requires-confirmation event type, a second user for the dynamic group + one with `allowDynamicBooking: false`, a phone-only event type (bookingFields with `attendeePhoneNumber` required and email not required), and for C1 a user whose schedule has NO weekly availability and only date overrides (`Availability` rows with `date` set and `days: []`) plus an `OutOfOfficeEntry` covering one of those dates.
- Minimum set: C1 (web: the booking page of the date-override-only host for the month containing the OOO day: is the day still bookable, no OOO marker), C2 (`GET /v2/slots?usernames=<a>,<b>&format=range&start=...&end=...` with `cal-api-version: 2024-09-04`: `end === start` on every slot; and with `&duration=30` to show the workaround), C3 (book a seat on the seated event type, then `POST /v2/bookings/{uid}/attendees` -> 201; then `GET /v2/bookings/{uid}` and the slots endpoint to read seat counts; record `attendees`, `seatsRemaining`), C4 (reserve a slot `POST /v2/slots/reservations` anonymously, then anonymous `PATCH /v2/slots/reservations/{uid}` with `reservationDuration` -> 200; also anonymous `GET` and `DELETE` on the same uid; and with an unrelated user's API key), C5 (book a requires-confirmation event type -> PENDING; `POST /v2/bookings/{uid}/reschedule` with `rescheduledBy: <attendee email>` using the host's API key -> status `accepted` vs expected pending; if cheap also try it without any auth), C6 (decline a pending booking, then reschedule it -> 404 `Could not find original booking`), C7 (reserve a slot then immediately `GET /v2/slots` for that day: the slot still listed; repeat after 2.5 s), C8 (`POST /v2/bookings/{uid}/reassign` and `/reassign/{userId}` on a normal booking -> 200 with `reassignedTo` and nothing changed), C9 (burst 130 requests in under a minute with one API key against a cheap authenticated endpoint, e.g. `GET /v2/me` or `/v2/bookings`; record every status and the `X-RateLimit-Limit/Remaining/Reset` headers of the first, 120th and last; with and without the API key), C10 (web: `/<userA>+<userB>` where userB has `allowDynamicBooking: false`: is a bookable page rendered), C11 (web: booking form of the phone-only event type: accessibility snapshot of the phone input, its accessible name; clicking the visible label), C12 (web: `?attendeePhoneNumber=%2B14155551234` and `?phone=...` prefill on the same form: the input value; expected to be FIXED on main by PR #29740), C13 (web: `?location={"value":"attendeeInPerson","optionValue":"Delhi"}` prefill: option selected, address input value).
- Also cheap if the API is up: C14/C15 (`GET /v2/slots` default format: object items not strings; seated slot keys `seatsBooked/seatsRemaining/seatsTotal`, no `attendeesCount`).
