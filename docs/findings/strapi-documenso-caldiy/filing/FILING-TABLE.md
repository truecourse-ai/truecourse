# Filing table (re-verified 2026-08-19)

Generated by `filing/tools/build-table.py` from FINDINGS-INDEX.json, reverify/, live/ and the issue drafts' front matter. Edit the sources, not this file.

Columns: still present on the default branch today (from the source re-check, with the fix commit when fixed); live re-run verdict on today's build; new upstream reports since 2026-08-15 (and cited items whose state changed); route; confidence; draft file. Per-finding filer notes and the live-run one-liners are in the section below the table.

| id | repo | title | still present today | live re-run | new upstream since 2026-08-15 | route | conf | draft |
|---|---|---|---|---|---|---|---|---|
| S1 | Strapi | Admin-token permissions are deleted at every restart | yes (source unchanged) | still reproduces on c7dbadd4fe | none | public issue | high | issues/strapi/S1-admin-token-permissions-deleted-at-restart.md |
| S2 | Strapi | set + connect in one relation write is accepted and connect is silently dropped | yes (source unchanged) | still reproduces on c7dbadd4fe | #27395 (open, related); #27115 (open, related) \| cited changed: strapi/strapi#26990 -> open | public issue | high | issues/strapi/S2-set-plus-connect-drops-connect.md |
| S3 | Strapi | set: null (clear all relations) is a no-op that returns success | yes (source unchanged) | still reproduces on c7dbadd4fe | #27395 (open, related); #27115 (open, related) \| cited changed: strapi/strapi#26990 -> open | public issue | high | issues/strapi/S3-set-null-does-not-clear-relations.md |
| S4 | Strapi | Every MCP create/update reply reports to-many relations as [] | yes (source unchanged) | still reproduces on c7dbadd4fe | #27395 (open, related); #27115 (open, related) \| cited changed: strapi/strapi#26990 -> open | public issue | high | issues/strapi/S4-mcp-write-replies-report-empty-relations.md |
| S5 | Strapi | $null / $notNull filters reject the boolean the rest of Strapi requires | yes (source unchanged) | still reproduces on c7dbadd4fe | #27395 (open, related); #27115 (open, related) \| cited changed: strapi/strapi#26990 -> open | public issue | high | issues/strapi/S5-null-notnull-filters-reject-boolean.md |
| S6 | Strapi | Raw SQL with bound values is echoed to the MCP client on a write error | yes (source unchanged) | still reproduces on c7dbadd4fe | #27395 (open, related); #27115 (open, related) | security disclosure | medium | issues/strapi/S6-mcp-error-echoes-raw-sql.md |
| S7 | Strapi | list_* default page size is 10; the tool's own schema and the docs say 25 | yes (source unchanged) | still reproduces on c7dbadd4fe | #27395 (open, related); #27115 (open, related) \| cited changed: strapi/strapi#26990 -> open | public issue | high | issues/strapi/S7-mcp-list-default-page-size.md |
| S8 | Strapi | DELETE /api/:plural/:id?status=draft answers 500; ?status=published deletes the draft too | yes (source unchanged) | still reproduces on c7dbadd4fe | none | public issue | high | issues/strapi/S8-delete-with-status-parameter.md |
| S9 | Strapi | Any non-GET request to an unrouted path answers 405 with Allow: HEAD, GET and a plain-text body | yes (source unchanged) | still reproduces on c7dbadd4fe | #25019 (closed, related) | public issue | medium | issues/strapi/S9-unrouted-paths-answer-405.md |
| S10 | Strapi | The bulk-unpublish confirmation dialog says Confirm, not Unpublish | yes (source unchanged) | still reproduces on c7dbadd4fe | none | public issue | high | issues/strapi/S10-bulk-unpublish-dialog-confirm-label.md |
| S11 | Strapi | Admin-token secret key is documented as shown only once; it is returned on every read (plus a verified-not-exercised escalation) | yes (source unchanged); doc text still present | still reproduces on c7dbadd4fe | none | docs repo issue | high | issues/strapi-documentation/S11-admin-token-key-shown-once.md, issues/strapi/S11-admin-token-key-escalation.md |
| S12 | Strapi | The MCP tool table scopes discard_draft to the publish permission; it is mapped to explorer.update | yes (source unchanged); doc text still present | not re-run | none | docs repo issue | high | issues/strapi-documentation/S12-mcp-discard-draft-permission-cell.md |
| S13 | Strapi | Docs name Delete document / Delete locale buttons that were renamed in 2024 | yes (source unchanged); doc text still present | not re-run | none | docs repo issue | high | issues/strapi-documentation/S13-delete-document-button-labels.md |
| S14 | Strapi | Error-handling doc inverts UnauthorizedError and ForbiddenError | yes (source unchanged); doc text still present | not re-run | none | docs repo issue | high | issues/strapi-documentation/S14-forbidden-unauthorized-descriptions-swapped.md |
| S15 | Strapi | REST reference prints 404 message as Document not found and 403 as Forbidden access; the wire says Not Found / Forbidden | yes (source unchanged); doc text still present | still reproduces on c7dbadd4fe | #18673 (closed, related) | docs repo issue | high | issues/strapi-documentation/S15-error-messages-never-returned.md |
| S16 | Strapi | The single document object is documented with a meta key it never has | yes (source unchanged); doc text still present | still reproduces on c7dbadd4fe | none | docs repo issue | high | issues/strapi-documentation/S16-single-document-meta-key.md |
| S17 | Strapi | pageSize ceiling 100 vs examples/getstarted maxLimit 30 (environment artifact, do not file) | yes (source unchanged) | not re-run | cited changed: strapi/strapi#22483 -> open | skip: environment artifact | high | none |
| D1 | Documenso | A sent (PENDING) document accepts new recipients (and recipient edits/deletes, field creates/updates) | yes (source unchanged) | still reproduces on v2.17.0 | #3188 (open, related); #3189 (open, related); #3186 (open, related) \| cited changed: documenso/documenso#3136 -> open | security disclosure | high | issues/documenso/D1-pending-document-accepts-new-recipients.md |
| D2a | Documenso | POST /envelope/field/update-many moves nothing (route reads page/positionX/positionY, service writes pageNumber/pageX/pageY) | yes (source unchanged) | still reproduces on v2.17.0 | cited changed: documenso/documenso#3136 -> open | comment on existing PR #3136 | high | issues/documenso/D2a-update-many-drops-coordinates.md |
| D2b | Documenso | update-many wipes the field's fieldMeta to type defaults on any partial update | yes (source unchanged) | still reproduces on v2.17.0 | cited changed: documenso/documenso#3136 -> open | public issue | high | issues/documenso/D2b-update-many-wipes-fieldmeta.md |
| D3 | Documenso | An ASSISTANT recipient is accepted on a PARALLEL envelope | yes (source unchanged) | still reproduces on v2.17.0 | none | public issue | high | issues/documenso/D3-assistant-accepted-on-parallel-envelope.md |
| D4 | Documenso | POST .../reject returns 200, the next GET still says PENDING (status written by an async job) | yes (source unchanged) | still reproduces on v2.17.0 | #3180 (open, related); #3187 (open, related); #3232 (closed, related); #3234 (closed, related) | public issue | high | issues/documenso/D4-reject-leaves-envelope-pending.md |
| D5 | Documenso | required: true + readOnly: true on a field is accepted by the API | yes (source unchanged) | still reproduces on v2.17.0 | #3224 (open, related); #3170 (open, related) | public issue | high | issues/documenso/D5-required-and-readonly-field-accepted.md |
| D6 | Documenso | Rate limit advertised by v2.16.0 is 100 req/min; docs say 1000 (fixed on main by PR #3081) | no: fixed by #3081 (2026-07-21, release: v2.17.0) | fixed on v2.17.0 | cited changed: documenso/documenso#3133 -> merged | skip: fixed (in v2.17.0) | high | none |
| D7 | Documenso | After a distribution-method None send, the document page shows no signing link | yes (source unchanged) | still reproduces on v2.17.0 | #3197 (open, related) | public issue | medium | issues/documenso/D7-none-send-shows-no-signing-link.md |
| D8 | Documenso | The API-token expiry chooser has no accessible name | yes (source unchanged) | still reproduces on v2.17.0 | #3230 (open, related); #3201 (open, related) | public issue | high | issues/documenso/D8-token-expiry-select-has-no-accessible-name.md |
| D9 | Documenso | The Copy Signing Links control and the three-dots trigger have no accessible name | yes (source unchanged) | still reproduces on v2.17.0 | #3201 (open, related) | public issue | high | issues/documenso/D9-signing-link-controls-have-no-accessible-name.md |
| D10 | Documenso | The documented v2 error format {error, code, statusCode} is never emitted; top-level INTERNAL_SERVER_ERROR rider | yes (source unchanged); doc text still present | still reproduces on v2.17.0 | #3219 (open, related) \| cited changed: documenso/documenso#3133 -> merged | public issue | high | issues/documenso/D10-documented-v2-error-body-is-never-emitted.md |
| D11 | Documenso | Field coordinates documented as number in responses; the wire sends JSON strings | yes (source unchanged); doc text still present | still reproduces on v2.17.0 | cited changed: documenso/documenso#3136 -> open | comment on existing PR #3136 | high | issues/documenso/D11-D12-fields-page-request-shape-and-string-coordinates.md |
| D12 | Documenso | The Fields guide's create/update bodies and response key are the legacy /document/field/* shape | yes (source unchanged); doc text still present | not re-run | cited changed: documenso/documenso#3136 -> open | comment on existing PR 3136 | high | none |
| D13 | Documenso | POST /envelope/get-many is documented as taking {envelopeIds}; it takes {ids: {type, ids}} | no: fixed by #3135 (2026-08-19, release: v2.17.0); doc text changed | fixed on v2.17.0 | cited changed: documenso/documenso#3135 -> merged; documenso/documenso#3134 -> merged; documenso/documenso#3133 -> merged | skip: fixed (in v2.17.0) | high | none |
| D14 | Documenso | List responses documented with pagination: {page, perPage, totalPages, totalItems}; the counters are flat | yes (source unchanged); doc text still present | still reproduces on v2.17.0 | cited changed: documenso/documenso#3135 -> merged; documenso/documenso#3137 -> open | comment on existing PR #3137 | high | issues/documenso/D14-pagination-wrapper-still-on-four-pages.md |
| D15 | Documenso | Docs say a token without access gets 403; cross-tenant reads answer 404 by design | yes (source unchanged); doc text still present | still reproduces on v2.17.0 | none | public issue | high | issues/documenso/D15-authentication-page-promises-403-api-answers-404.md |
| D16 | Documenso | Docs say clicking a document opens the editor; it opens the read-only page | yes (source unchanged); doc text still present | not re-run | none | public issue | high | issues/documenso/D16-add-recipients-page-says-clicking-opens-editor.md |
| D17 | Documenso | Download version=signed on a pending envelope (test defect; filename note only) | yes (source unchanged) | not re-run | none | skip: test defect | high | none |
| C1 | Cal.diy | An out-of-office period is ignored when availability comes from date overrides | yes (source unchanged) | attempted, blocked (disk) | #29951 (open, related) | public issue | medium | issues/caldiy/C1-out-of-office-ignored-date-overrides.md |
| C2 | Cal.diy | GET /v2/slots?usernames=a,b&format=range returns every slot with end === start | yes (source unchanged) | attempted, blocked (disk) | none | public issue | high | issues/caldiy/C2-dynamic-slots-range-zero-length.md |
| C3 | Cal.diy | POST /v2/bookings/{uid}/attendees on a seated booking returns 201 and attaches a seat-less attendee | yes (source unchanged) | attempted, blocked (disk) | none | public issue | high | issues/caldiy/C3-seated-booking-accepts-seatless-attendee.md |
| C4 | Cal.diy | PATCH /v2/slots/reservations/{uid} accepts reservationDuration from an anonymous caller; GET/PATCH/DELETE unguarded | yes (source unchanged) | attempted, blocked (disk) | none | security disclosure | high | issues/caldiy/C4-unguarded-slot-reservation-endpoints.md |
| C5 | Cal.diy | Rescheduling a PENDING booking with rescheduledBy = the attendee returns accepted | yes (source unchanged) | attempted, blocked (disk) | none | public issue | high | issues/caldiy/C5-pending-reschedule-auto-confirmed-by-caller.md |
| C6 | Cal.diy | Rescheduling a declined booking answers 404 Could not find original booking instead of 400 | yes (source unchanged) | attempted, blocked (disk) | none | public issue | high | issues/caldiy/C6-rejected-booking-reschedule-returns-404.md |
| C7 | Cal.diy | A slot reservation is not reflected in the very next GET /v2/slots (2 s cache) | yes (source unchanged) | attempted, blocked (disk) | none | public issue | high | issues/caldiy/C7-reservation-not-reflected-in-next-slots.md |
| C8 | Cal.diy | Both round-robin reassign endpoints return 200 success with a reassignedTo object and do nothing (fork stubs) | yes (source unchanged) | attempted, blocked (disk) | none | public issue | high | issues/caldiy/C8-reassign-endpoints-return-success-noop.md |
| C9 | Cal.diy | The documented 120 requests/min per API key is not enforced | yes (source unchanged) | attempted, blocked (disk) | none | security disclosure | medium | issues/caldiy/C9-api-key-rate-limit-not-enforced.md |
| C10 | Cal.diy | A dynamic group link containing a user with allowDynamicBooking off still renders a bookable group page | yes (source unchanged) | attempted, blocked (disk) | #28688 (open, related); #28734 (open, related); #28789 (closed, related); #29273 (closed, related) | public issue | medium | issues/caldiy/C10-dynamic-booking-opt-out-ignored.md |
| C11 | Cal.diy | The booking form's phone control has no accessible name | yes (source changed, bug remains) | attempted, blocked (disk) | #29761 (open, related); #26367 (open, related) | public issue | high | issues/caldiy/C11-phone-input-has-no-accessible-name.md |
| C12 | Cal.diy | The documented attendeePhoneNumber= prefill never reaches the Phone question (fixed on main by PR #29740) | no: fixed by #29740 (2026-08-05, release: none) | attempted, blocked (disk) | none | skip: fixed (main only, no tag) | high | issues/caldiy/C12-SKIP-NOTE.md |
| C13 | Cal.diy | The docs' own attendee-address prefill URL leaves the address input empty | yes (source unchanged) | attempted, blocked (disk) | none | public issue | high | issues/caldiy/C13-attendee-address-prefill-is-dropped.md |
| C14 | Cal.diy | Default GET /v2/slots format documented as an array of strings; it is objects | yes (source unchanged); doc text still present | attempted, blocked (disk) | none | public issue | high | issues/caldiy/C14-C15-slots-response-schema-wrong.md |
| C15 | Cal.diy | Seated slots documented under attendeesCount; the emitter sends seatsBooked/seatsRemaining/seatsTotal | yes (source unchanged); doc text still present | attempted, blocked (disk) | none | public issue | high | none |
| C16 | Cal.diy | The help centre's worked prefill example uses the deleted /{user}/book route | yes (source unchanged); doc text still present | attempted, blocked (disk) | none | docs repo issue | high | issues/calcom-help/C16-prefill-example-uses-deleted-route.md |

## Per-finding notes

### S1 (Strapi) - Admin-token permissions are deleted at every restart

**Live re-run:** tools/list returned ["list_article","get_article"] before the restart and [] after it, with nothing else changed. GET /admin/admin-tokens/5 after the restart shows adminPermissions: [], so the permission row is deleted rather than the ability being derived differently. The token still authenticates (tools/call answers 'Tool list_article disabled', not an auth error). No warning or log line accompanies the deletion. **Filer notes:** Not a security report despite being a permission bug: it fails closed (the token loses access, it does not gain any), so it is not a boundary violation under SECURITY.md and belongs in the public tracker. Reproduction for the BUG_REPORT.yml 'Steps to Reproduce' field, no MCP needed: POST /admin/admin-tokens with adminPermissions [{action: 'plugin::content-manager.explorer.read', subject: '<a localized content type>'}] and no properties, GET it back to confirm the grant, restart the server, GET again and observe adminPermissions empty. Strapi Version field: 5.52.0 (tested) and confirm it also reproduces on 5.52.1. The suggested fix belongs in filterPermissionsToRemove (queries.ts:90-123): skip the nil-property rule for apiToken-owned rows, mirroring the orphan branch at 147-154 that already special-cases them. **Re-evaluation:** The finding stands unchanged. No commit since the tested build touches any of the five culprit files, and the exact lines the review blamed are still where it left them, so the boot-time reap of apiToken-owned content-type permissions with empty properties is still live on develop and in v5.52.1. Two searches turned up nothing new: nobody has reported the restart wipe, and no PR is in flight on permission/queries.ts. The review's fixedAfter 'no' is still correct. Verdict, confidence (high) and scope are all unchanged. One thing a filer should carry over from the review's own scoping, because it decides whether a maintainer can reproduce: the wipe only bites a content-type permission stored with empty properties on a LOCALIZED content type. A non-localized type survives (the i18n 'locales' check returns false so not every applicable property is nil), an explicit properties.fields array survives, and settings/plugin permissions survive. Nothing about MCP is required to reproduce; MCP was only the readout.

### S2 (Strapi) - set + connect in one relation write is accepted and connect is silently dropped

**Live re-run:** update_article with categories {set:[cat1], connect:[cat2]} answered 200 with no isError and updatedAt advanced. The get_article read-back, which the original run never reached, holds cat1 only, confirming connect was silently discarded. **Filer notes:** File the silent acceptance plus the dropped write, not a specific error string: the doc states the constraint but never promises a particular error, so the scenario's expected message ('Input validation error: Invalid arguments for tool update_article') is the test's own choice and should not appear in the Expected Behavior field as if it were documented. This is best filed together with S3, since both live one line apart in toAssocs and a maintainer fixing one will look at the other. The evidence transcript aborted at this step, so the run has no read-back proving only cat1 survived; that half is a code-path reading (entity-manager/index.ts:162), and a filer should either say so or produce the read-back. **Re-evaluation:** Stands unchanged. Neither the schema nor the database short circuit moved, so a to-many payload carrying both set and connect is still accepted and connect is still discarded before the write. The one open PR that edits data-schema.ts, #27115, was pushed again on 2026-08-18 but its diff has no hunk covering the relation object (grep for 'Mutually exclusive' across its diff finds nothing), so it does not add the missing refinement. PR #26990 does not touch data-schema.ts at all. Nothing new was filed. The review's fixedAfter and upstream claims are still accurate, including its refinement that the drop is decided in the 2022-era database layer rather than in MCP code.

### S3 (Strapi) - set: null (clear all relations) is a no-op that returns success

**Live re-run:** categories {set: null} answered 200 with no isError and updatedAt advanced by 71 ms, and the read-back still returns the category. Matches the sibling run 2026-08-14T15-15-28Z_32215874 on every observable. **Filer notes:** Best filed as one issue with S2 (both are toAssocs, lines 162 and 149-180). Frame it as a document-service bug surfaced by MCP, not an MCP bug: the same no-op hits any caller passing { set: null } to the Document Service API, and the asymmetry is worth quoting, since 'categories: null' DOES clear (line 158 yields { set: null }) while 'categories: { set: null }' does not. The one-line fix (if ('set' in data) instead of if (data?.set)) is load-bearing since v4 and needs regression tests; say so rather than presenting it as safe. Also note S4 compounds this: the write reply reports categories as [] anyway, so the success confirmation agrees with the caller's wrong belief that the field is now empty. **Re-evaluation:** Stands unchanged. The 2022-era truthiness check and its consumer are byte-identical to the tested build, so 'categories: { set: null }' still returns success, still bumps updatedAt and still clears nothing. No new issue or PR touches it. The review's fixedAfter 'no' is correct. Its two caveats still hold and matter for filing: the blame is the database package (PR #14327, 2022), not the MCP schema, so the fix also repairs the plain document-service API; and the evidence is NOT in the transcript the findings index points at (run 2026-08-14T15-18-58Z_4e4c9455 aborted at step 21 on the mutual-exclusivity step). The proof is the sibling run 2026-08-14T15-15-28Z_32215874, steps 21 and 22. A filer must pull the reproduction from that run or reproduce it fresh, otherwise the cited transcript does not show the defect.

### S4 (Strapi) - Every MCP create/update reply reports to-many relations as []

**Live re-run:** Three consecutive update_article replies all report "categories":[] while get_article at the identical updatedAt returns one category. Every write reply observed in the session was false. **Filer notes:** Cross-reference open PR #26990 in the issue body: it is actively reworking shape-relations.ts and its author is the right person to fold in either fix (pass a non-count populate from the MCP write handlers, or make reduceToIdentity refuse to translate { count: N } into []). Scope is broader than 'update': the same count-shaped default populate is used by create, publish, unpublish and discard_draft on collection types. Single types are NOT affected (single-type-handlers.ts:116-118 passes an explicit populate), which is a useful contrast for the report. The write itself is correct; only the reply is false, so this is a wrong-confirmation bug, not data loss. **Re-evaluation:** Stands unchanged, but with one material update the review could not have: open PR #26990 was pushed again on 2026-08-18 and it rewrites shape-relations.ts substantially (adding an inline-relation resolver and a shouldInline predicate). I checked whether it fixes this. It does not. Its first hunk on that file starts at line 98, which is reduceToIdentity's closing brace, so lines 1-97 including the '{ count: N } -> []' coercion at 83-84 are untouched context. Its collection-handlers.ts hunks stop at base line ~241, well before the update handler at 317-325, so the write path still takes the count-shaped default populate. Net: if #26990 merges as it stands, this defect survives it, and the file will have been rewritten around the faulty lines, which makes filing BEFORE that merge worthwhile so the author can fold the fix in. Everything else in the review holds: no new report, #26560 unchanged, fixedAfter 'no' still correct.

### S5 (Strapi) - $null / $notNull filters reject the boolean the rest of Strapi requires

**Live re-run:** $null:true and $notNull:true both rejected with MCP error -32602 'Invalid input at filters.$and[1].authorName'. The $notNull half, never executed by the original run, fails identically. $null:null, which the advertised schema does accept, returned the two documents that HAVE an author name, the exact complement of what was asked, with no error. **Filer notes:** Worth a short comment on PR #26990 as well as the issue, since that PR relocates the exact faulty line into buildScalarFieldFilter and its author can fix it in place. Scope for the report: broken on every scalar attribute type except boolean, not just on string fields (integer/decimal fields get number | number[] and reject true the same way); accepted only where valueSchema is z.boolean(). Two claims in the review are code-path readings with no evidence file behind them, so mark them as such or drop them: that {"$null": "true"} works via parseType's TRUTHY_INPUTS, and that {"$null": null} force-casts to false and silently runs whereNotNull (the inverse of the request). The second is the more damaging of the pair and is worth reproducing before it goes in a bug report. **Re-evaluation:** Stands unchanged, and I re-confirmed the review's most consequential claim about PR #26990 against the PR's current diff (it was pushed again on 2026-08-18, after the review). The PR does refactor the per-field builder into a new helper buildScalarFieldFilter, and that helper still contains the faulty line verbatim: 'const valueSchema = attributeTypeToFilterValue(attr)' followed by 'Object.fromEntries(FILTER_OPERATORS.map((op) => [op, valueSchema.optional()]))'. So merging #26990 would carry $null/$notNull forward broken, exactly as the review said, and would also extend the same bug to nested component fields. Nothing new was filed. The one genuinely new neighbour is issue #27395 (2026-08-17, open, severity: high, status: confirmed), a different MCP tool-schema defect; it is not a duplicate but it establishes that this class of report is accepted and triaged quickly.

### S6 (Strapi) - Raw SQL with bound values is echoed to the MCP client on a write error

**Live re-run:** With a BEGIN IMMEDIATE lock held on the sqlite file from a second process, MCP create_article returned the full insert statement with every bound value plus 'database is locked'. The same failure through POST /api/articles returned a scrubbed 500 with no SQL, which localises the fix to the MCP boundary. **Filer notes:** Filer must know four things. (1) The GHSA report has a MANDATORY template (Summary / Affected Versions / Vulnerability Details / Proof of Concept / Impact / optional CVSS 4.0 / AI Usage Disclosure) and NO file attachments; inline everything in fenced blocks. (2) AI-usage disclosure is required in the initial report: "If artificial intelligence tools (LLMs, AI-powered scanners, AI-assisted code analysis, agentic coding tools, etc.) were used in any part of discovering, validating, analyzing, or drafting your vulnerability report, you are required to disclose this", listing tools, stages, and confirming human verification. This finding came out of an agentic review, so that section is not optional. (3) Two exclusions are live risks and should be pre-empted in the Impact section: "Banner, version, or stack disclosure on public surfaces without a demonstrated downstream exploit" and CNA rule 4.1.2 "Conditions or behaviors that do not lead to a security impact". The strongest available framing is the self-contradiction: wrapSafeHandler's own docblock promises the client "a safe error response (no stack trace leak)" while tool-registry.ts defeats it, and Strapi's REST path scrubs the identical error via formatInternalError. (4) The same verbatim echo exists in prompt-registry.ts:91 and resource-registry.ts:93, so all three MCP capability kinds leak; include them or the fix will be partial. If triage declines it as non-security, refile as a public issue on the docblock contradiction. There is no bounty, and there is no response SLA (escalate by commenting on the GHSA thread after 14 days). **Re-evaluation:** Stands unchanged: the verbatim echo is byte-identical on develop and shipped in v5.52.1. Nothing has been filed. The one thing re-verification adds is the dev-mode question, which decides whether Strapi will even look at it: the MCP server is gated by server.mcp.enabled, not by autoReload, so this is NOT an 'only reproduces under strapi develop' finding and the report should say so in as many words. What the review did not establish, and what SECURITY.md demands, is a PoC showing the disclosure crosses a security boundary. The observed leak (an INSERT statement with the caller's own values plus the internal created_by_id) is largely the caller's own data plus table/column names an authenticated MCP client can derive from the content-type schemas anyway. The review's stronger claim, that an UPDATE or a unique-constraint violation can put another record's data in the message, is a code-path inference with no evidence file behind it. Verdict on the behavior stays real-defect; confidence that Strapi will accept it as a vulnerability is medium at best.

### S7 (Strapi) - list_* default page size is 10; the tool's own schema and the docs say 25

**Live re-run:** list_article with no pagination arguments returned pagination {page:1,pageSize:10,pageCount:1,total:3}, while the same server's tools/list advertises pageSize 'default: 25, max: 100' and REST on the same instance answers pageSize 25. **Filer notes:** File this on strapi/strapi, not strapi/documentation: the wrong number is inside the shipped product (input-schemas.ts:33 is what a client reads in tools/list), so the docs are downstream of the code and would only need editing if maintainers decide 10 is the intended default. Two supporting facts worth including because they close off the easy dismissals: the 10 is hardcoded in packages/core/utils/src/pagination.ts and unreachable by config, so no app setting explains it; and the MCP unit tests are green on 25 only because they mock documentManager.findPage and hardcode pagination: { page: 1, pageSize: 25 } in the mock's return value (mcp/__tests__/derive-content-type-mcp-tools.test.ts:128-133, 625-633), i.e. they assert the mock. Secondary observation for the same issue: the schema's max of 100 is enforced by Zod at the tool boundary while findPage passes maxLimit 1000, so the two ceilings disagree (the tighter one wins). Note also that the scenario aborted at the first failing step, so the max-100 and page-2 assertions never ran and are untested rather than passing. **Re-evaluation:** Stands unchanged. Both halves of the contradiction are still in the product (schema says 25, handler omits the key so withDefaultPagination falls to the hardcoded 10) and the doc sentence is still published. PR #26990 edits the very block that omits the keys, adding fields/populate to the same spread, and I confirmed from its current diff that 'pageSize !== undefined' appears as unchanged context in hunk @@ -90,15 +95,29 @@: it leaves the omission and the 25-vs-10 mismatch exactly as they are. Nothing new filed. The review's fixedAfter 'no' and its upstream notes are all still correct.

### S8 (Strapi) - DELETE /api/:plural/:id?status=draft answers 500; ?status=published deletes the draft too

**Live re-run:** DELETE ?status=draft answered 500 with 'Cannot delete a draft document' logged at repository.js:273 and the document intact. The ?status=published half, inferred from source in the review and observed here for the first time, answered 204 and left both the draft and the published version 404. **Filer notes:** There is a docs half too, but it belongs to whoever files S11-S16 style doc issues, not in the same report: docusaurus/docs/cms/features/draft-and-publish.md:197 promises status can scope a delete, and it cannot (draft throws, published is dropped by omit('status') so every version goes). The product's own detailed pages agree with the code, not with that sentence, so the doc fix is to drop 'and deleted' or to spell out that a delete always removes every version. Keep the product issue about the code: status is Picked into the Delete params type (packages/core/types/src/modules/documents/params/document-engine.ts:51-63) and passes validateQuery + sanitizeQuery in the collection-type controller, so the product accepts the parameter at the edge and then 500s on one of its two legal values. No data was lost in the observed run because the throw precedes the query. **Re-evaluation:** Stands unchanged. The bare Error is still there, the omit('status') on both queries is still there, and nobody has filed either half. The review's fixedAfter 'no' is correct, and its claim that the string 'Cannot delete a draft document' appears exactly once in the tree (the source line, no test) still holds on develop. The finding has two separable parts and a filer should keep them apart, because they have different severities: the 500-instead-of-400 is a wrong error type (nine sibling guards in the same file throw errors.ValidationError), while status=published silently deleting the draft too is the data-affecting half and is the one that will get attention. Only the first was observed in the run; the second is a code-path reading (omit('status') at 374/383 plus transform/query.ts adding no publishedAt condition) and should be reproduced before it goes in the report.

### S9 (Strapi) - Any non-GET request to an unrouted path answers 405 with Allow: HEAD, GET and a plain-text body

**Live re-run:** DELETE and POST on an unrouted path both answer 405 with Allow: HEAD, GET and a text/plain 'Method Not Allowed', while GET on the same path answers 404 with the documented JSON envelope. DELETE /api/articles answers 405 with a truthful Allow: HEAD, GET, POST, which supports the review's note about step 4 of the yaml. **Filer notes:** Frame it as the inconsistency and the wrong Allow header, not as 'DELETE should be 404', or it will be closed like its four predecessors. Cite #11756, #15024, #24197 and #25019 in the body as evidence of recurring cost. Second thing a filer must fix before filing: the scenario's step 4 (DELETE /api/articles with no documentId, expecting 404) never ran, and 405 would be the CORRECT answer there since /api/articles is a real resource supporting GET, HEAD and POST; do not present step 4 as a defect. Suggested fix to offer: register the koa-static handler as plain app-level middleware (or a method-agnostic layer) rather than a GET router route, so an unrouted path stops advertising GET. Note for scope: this affects every non-GET/HEAD method on every unrouted path in every Strapi app, not just the content API. **Re-evaluation:** Stands unchanged on the source side, and nothing new has been filed. The re-verification adds a fourth prior instance the review missed, #25019 (2025-12-11, 405 on POST /admin/register-admin), closed the same day with no code change, which reinforces the pattern that all four reports were resolved as user confusion rather than as the router behaviour. None of them is a duplicate of this finding: none names the catch-all GET route or the allowedMethods interaction, so this is still unreported as such. The review's own confidence of medium is right and should be preserved. The weak point is the doc binding: the REST reference never states a status code for an unknown pluralApiId, so 'expect 404' is the scenario's inference. What carries the report is the product's internal inconsistency, observed in the same run: GET on that exact path returns 404 with the documented {data,error} envelope while DELETE returns 405 with a plain-text body and an Allow header (HEAD, GET) that is factually wrong, since GET is a 404 there too.

### S10 (Strapi) - The bulk-unpublish confirmation dialog says Confirm, not Unpublish

**Live re-run:** The bulk-unpublish alertdialog reads 'Confirmation' / 'Are you sure you want to unpublish these entries?' with buttons Cancel and Confirm and zero buttons named Unpublish. Clicking Confirm does unpublish both entries, so the defect is label-only. The bulk-publish dialog does end on a button named Publish. A guided-tour popover on a fresh admin user had to be dismissed first. **Filer notes:** This is a strapi/strapi issue, NOT a docs issue: the docs are right and the UI regressed. The fix is one word, confirmButton -> confirmLabel at packages/core/content-manager/admin/src/pages/ListView/components/BulkActions/Actions.tsx:203, so a PR is cheap if the filer wants one (CLA required, branch from develop, yarn test:front + yarn lint). Two things worth putting in the body: the type checker cannot catch this class of typo in the current shape (freshness is lost on an object literal returned from a contextually typed arrow function, so excess-property checking is skipped; annotating the descriptor explicitly restores TS2353), which suggests auditing the other DescriptionComponent implementations for silently dropped keys; and the action's onConfirm is intact, so the unpublish still works when Confirm is clicked. Label-only, no data loss. The localization angle is the strongest impact argument: the confirm button loses its app.utils.unpublish string in every locale and falls back to app.components.Button.confirm. **Re-evaluation:** Stands unchanged. The dead confirmButton property and the renderer that ignores it are both still on develop and shipped in v5.52.1, and the one commit that did touch DocumentActions.tsx since the tested build (efe5a9dc17, PR #26900) only edits PublishAction's draft-relations counting. Nothing has been filed. The doc sentence the finding binds to is also still live: strapi/documentation origin/main has 'In the confirmation dialog box, confirm your choice by clicking again on the **Unpublish** button' at docusaurus/docs/cms/features/draft-and-publish.md line 193. The review's judgement that the docs are correct here and the UI regressed still holds, and it matters for routing: the sibling bulk-publish dialog really does render 'Publish', and the single-entry unpublish dialog really does say 'Confirm', so the doc author described real per-dialog labels. Verdict, confidence (high) and low severity unchanged.

### S11 (Strapi) - Admin-token secret key is documented as shown only once; it is returned on every read (plus a verified-not-exercised escalation)

**Live re-run:** Wider than filed. The plaintext accessKey comes back on every GET /admin/admin-tokens/:id, and the escalation the review flagged as unexercised is confirmed: an admin token whose only permission is admin::admin-tokens.read read a sibling token's full 256-character key, then served six content-manager MCP tools it had none of and created an article. Recommend re-routing as a security report. **Filer notes:** Two-part finding, and only one part is filable today. Part 1 (doc bug, file now): the caution at docusaurus/docs/cms/features/admin-tokens.md line 89 is wrong on both factual clauses, plus the `apiToken.secrets.encryptionKey` typo at line 49 of the same page. Part 2 (security escalation, HOLD): the sibling-token readback is verified in source but was never exercised by the run. The scenario transcript shows token ids 5, 6, 7, 8 and 10 all owned by user id 2, so one request (GET /admin/admin-tokens/6 with the step-2 token's bearer) would settle it. Do not open a security advisory without that request: SECURITY.md requires a PoC showing actual access to sensitive data. Also note strapi/documentation runs an automated 'docs self-healing' pipeline (.github/workflows/docs-self-healing.yml plus .github/prompts/docs-self-healing-{triage,router,drafter}.md) that opens doc PRs and records rejections in .github/self-healing-ignore.json; a filer should expect bot triage on any docs issue. **Re-evaluation:** The finding stands unchanged on both sides. The product code is byte-identical to the tested build (no commit touched packages/core/admin/server/src at all since 2026-08-13, including through the v5.52.1 release), and the doc caution is verbatim on strapi/documentation main. Every factual claim in the review's fixedAfter block re-verifies today. Nothing changes the verdict, confidence or scope of the doc half: the caution's first two clauses are false in a default install and the third (owner scoping against other admin users) is honored. The one thing that has NOT advanced is the escalation: a token holding only admin::admin-tokens.read reading back a SIBLING token's plaintext key is still established from source only, not exercised. I re-read routes/admin-tokens.ts and strategies/admin-token.ts at today's head and the reasoning holds exactly as written, but reading code is not a proof of concept, and Strapi's SECURITY.md makes a PoC a hard requirement. So the doc half is ready to file now and the security half is not.

### S12 (Strapi) - The MCP tool table scopes discard_draft to the publish permission; it is mapped to explorer.update

**Filer notes:** A fix must touch BOTH rows (233 collection types, 244 single types); patching only the first leaves the single-type table wrong. The correct cell value is `update`, corroborated by three independent code paths: the MCP tool policy (derive-content-type-mcp-tools.ts:296/504 -> ACTIONS.discard), the admin REST route policy for discard, and the admin UI Discard button gated on canUpdate. Worth mentioning in the same issue that `unpublish` has no permission of its own (ACTIONS.unpublish === ACTIONS.publish), so `publish` always grants both tools. The BUG_REPORT template's 'Related issue(s)/PR(s)' field should cite strapi/documentation#3194 as the origin of the cell. **Re-evaluation:** The finding stands exactly as written. Neither side moved: the ACTIONS map and the MCP tool auth policies are unchanged on develop through v5.52.1, and both wrong table cells are verbatim on strapi/documentation main at lines 233 and 244. Every claim in the review's fixedAfter block re-verifies. The one new piece of context is strapi/strapi#27395 (opened 2026-08-17), a separate MCP defect about the advertised JSON Schema dialect; it confirms the MCP surface is getting outside scrutiny right now but has no bearing on this finding's verdict, confidence or scope. Verdict remains doc-bug, confidence high, scope two cells in one file.

### S13 (Strapi) - Docs name Delete document / Delete locale buttons that were renamed in 2024

**Filer notes:** The 'Link to the documentation page' field should be https://docs.strapi.io/cms/features/content-manager (Deleting content section). Fix is three spots in one file: line 547 and line 559 ('Delete document' -> 'Delete entry (all locales)', 'Delete locale' -> 'Delete entry (<locale name>)'), plus line 86's 'Delete locale icon' in the list-view bullet list. Mention that without i18n on the content type the ICU 'other' branch renders plain 'Delete entry', which is why the label is conditional. Cite strapi/strapi#20812 and #21182 in the 'Related issue(s)/PR(s)' field as the paper trail for the rename. Caveat unchanged from the review: only the English bundle was inspected, so localized admin translations may still carry the old strings (that would be a separate strapi/strapi issue, not a docs one). **Re-evaluation:** The finding stands, unchanged on both sides. The admin UI still renders 'Delete entry (all locales)' and 'Delete entry ({locale})' on today's develop (no commit touched either component or either en.json since the tested build), and strapi/documentation main still instructs the reader to click 'Delete document' and 'Delete locale' at lines 547 and 559, now roughly 23 months after PR #21182 renamed them. Both cited upstream items are in exactly the state the review recorded and neither has had activity since. Nothing changes the verdict (doc-bug), the confidence (high) or the scope. One scope note the filer should carry forward: this is ONE doc bug with two evidenced surfaces, the edit-view header menu (scenario the-edit-view-menu-has-no-delete-document-button.web.1, step 8) and the list-view row menu (the-list-row-menu-has-no-delete-document-button.web.1, step 6), because DeleteAction declares position: ['header', 'table-row'] and renders in both. A single docs issue with both screenshots covers it.

### S14 (Strapi) - Error-handling doc inverts UnauthorizedError and ForbiddenError

**Filer notes:** The fix is a straight swap of the two TabItem descriptions plus, ideally, naming the HTTP status each class maps to, since packages/core/core/src/services/errors.ts is the authority (UnauthorizedError -> 401, ForbiddenError -> 403) and the page never states the mapping. Corroborating detail for the issue body: PolicyError extends ForbiddenError (packages/core/utils/src/errors.ts:132) and the same page at line 276 already recommends PolicyError for insufficient permissions, so the page contradicts itself two paragraphs apart. Do NOT frame this as a 403-should-be-401 report: strapi/strapi#9512 and #18782 already cover that (different, unauthenticated case) and #18782 was closed as not planned; conflating them is what got those reports dismissed. This finding and S15 both land in error-handling.md and could reasonably be filed as one docs issue with two sections, but they are independent defects (S14 is the inverted class semantics at lines 407/425; S15's doc half includes the 'returns the default message' note at line 418). **Re-evaluation:** The finding stands unchanged. Neither the class-to-status mapping nor the error classes moved on develop through v5.52.1, and both inverted sentences are verbatim on strapi/documentation main at lines 407 and 425. All four cited items are in exactly the state the review recorded, with no activity since. The review's framing correction also still holds and should survive into the filing: the run sent a VALID read-only token, so 403 is the HTTP-correct answer and the product is right; only the doc prose has the two classes the wrong way round. The searches turned up one new-ish neighbour, strapi/strapi#27247 (opened 2026-08-04, a generic Forbidden error when landing on a locale-restricted content type), but it is a product bug in the i18n permission path and does not report or fix this doc inversion. Verdict doc-bug, confidence high, scope two adjacent paragraphs in one file.

### S15 (Strapi) - REST reference prints 404 message as Document not found and 403 as Forbidden access; the wire says Not Found / Forbidden

**Live re-run:** GET an unknown documentId answers {"status":404,"name":"NotFoundError","message":"Not Found","details":{}} where the docs print 'Document not found'. A write with the read-only token answers message 'Forbidden' where the docs promise 'Forbidden access'. An anonymous read answers the same 403 body, so the docs' Unauthorized/Forbidden split matches neither case. **Filer notes:** Two separate edits in two files. (1) rest.md line 297: '"message": "Document not found"' -> '"message": "Not Found"', matching the surrounding component's own statusText prop; the real body also carries "details": {}. Note for the doc author that the product genuinely has three 404 wordings for the same conceptual failure and only one is reachable through /api/:pluralApiId/:documentId: public REST answers 'Not Found', the content-manager admin API and MCP handlers say 'Document not found', and the bare @strapi/utils class default is 'Entity not found'. (2) error-handling.md line 418: 'Instead, the API returns the default message' -> the API returns the generic HTTP status text ('Forbidden'), and say that the suppression applies only to ForbiddenError raised during route authentication/authorization; a ForbiddenError thrown from a controller, service or lifecycle keeps its message. Cite strapi/strapi#18672 (the closed report of the identical 403 body) and #18673 (the maintainer's 'intended behavior' comment) in the 'Related issue(s)/PR(s)' field. This finding and S14 both touch error-handling.md; if filed together, keep them as two clearly separated sections since they are independent defects. **Re-evaluation:** The finding stands on both halves, and it got stronger rather than weaker. Nothing moved: the 404 fallthrough and the ctx.forbidden() discard are byte-identical on develop through v5.52.1, rest.md still prints '"message": "Document not found"' at line 297 under a statusText that says 'Not Found', and error-handling.md still claims at line 418 that the API returns the class default message. Every cited item is in the state the review recorded, with no activity since. The one genuinely new piece of evidence is strapi/strapi#18673, the closed community PR that tried to make the classes return their message: maintainer innerdvations answered 'I also believe that this is intended behavior to suppress those specific error messages', which is Strapi stating on the record that the suppression is deliberate. That resolves the ambiguity the review flagged at medium confidence for the 403 half, so I would now raise that half from medium to high: the code is intended, the doc sentence is the defect, and the correct repair is to the doc, not to compose-endpoint.ts. Scope note for the filer: S15 is one finding with two doc surfaces (a 404 message in rest.md and a 403 message note in error-handling.md); both are the same underlying pattern, the docs printing a message string the wire has never emitted.

### S16 (Strapi) - The single document object is documented with a meta key it never has

**Live re-run:** GET /api/articles/:documentId returns data keys id, documentId and the flattened attributes only. meta appears exactly once, as a sibling of data, and is {}. **Filer notes:** One-line fix at docusaurus/docs/cms/api/rest.md:118. The page's own JSON examples already agree with the product (meta always appears beside data, never inside a document), which makes this an easy, low-controversy docs issue. S15's rest.md edit (the 404 message at line 297) is in the same file, so a filer may want to open one strapi/documentation issue covering both rest.md defects; keep them as separate numbered items in the body since they have different causes. Cite strapi/documentation#924 under 'Related issue(s)/PR(s)' only as context, not as a duplicate. **Re-evaluation:** The finding stands unchanged. The product side has not moved (no commit in the core-api controller directory since the tested build, so responses are still shaped { data, meta } with meta a sibling of data), and the misplaced bullet is verbatim at rest.md line 118 on strapi/documentation main. The single cited item, strapi/documentation#924, is in exactly the state the review recorded and has had no activity since 2022. No new issue or PR reports the misplaced bullet. Verdict doc-bug, confidence high, scope one line. The fix remains trivial: de-indent line 118 to the top level or delete it, since line 122 already documents meta correctly. Worth carrying the review's broader observation into the issue body, that the same list survived the v4-to-v5 rewrite unaudited and the whole Requests section deserves a pass against a live Strapi 5 response.

### S17 (Strapi) - pageSize ceiling 100 vs examples/getstarted maxLimit 30 (environment artifact, do not file)

**Filer notes:** Do not file. The only actionable item is on the harness side, not upstream: this flow can only be validated against an app whose api.rest.maxLimit is the scaffolded 100, so either run it against a create-strapi-app scaffold, override maxLimit for the fixture app, or drop the flow as unverifiable in this environment. If anyone later decides the rest.md wording is worth a one-line qualifier ('max `100`, configurable via `api.rest.maxLimit`'), that would be a separate, very low-priority strapi/documentation issue and should not be attached to this finding. **Re-evaluation:** Confirmed as an environment artifact, and nothing has changed. examples/getstarted/config/api.js still pins maxLimit: 30 at today's head, the pagination code paths are untouched since the tested build, and the generator templates still ship maxLimit: 100, so the documented 25/100 pair holds on any app produced by create-strapi-app. The red is entirely an artifact of running the flow against Strapi's in-repo demo app, whose lower ceiling has been deliberate since 2021. There is no defect to file against either strapi/strapi or strapi/documentation. Two things the review said that remain true and should not be upgraded into filings: step 1 of the scenario (pageSize=150) is not a real check in this environment because 30 happens to be under 100, and the 'max 100 is really a project-config value' imprecision on rest.md line 151 is a nit the review already judged not worth filing on its own. The only state change in the citedUpstream set is cosmetic: strapi/strapi#22483 got a stale-bot bump and a one-line reporter reply on 2026-08-17, still open, still unaddressed by maintainers, still about clamp-versus-error rather than the documented ceiling.

### D1 (Documenso) - A sent (PENDING) document accepts new recipients (and recipient edits/deletes, field creates/updates)

**Live re-run:** Wider than the original run could show. On a PENDING envelope: recipient create-many 200, recipient update-many 200 (email rewritten, signing token unchanged), recipient delete 200 (cascades the recipient's signature field away), field create-many 200, field update-many 200, field delete 200, envelope update/rename 200. Only envelope/item/delete refuses, with 400 'Envelope item is not editable'. Scenario steps 8-12 executed for the first time; the review's source-level-inference caveat can be dropped. **Filer notes:** The security routing is a judgement call, not a slam dunk, and the filer should decide deliberately. Arguments for the advisory form: it is an unenforced authorization rule on a first-party API that lets a token holder add a signing party to a document other parties already agreed to sign, which lands on the signing certificate; SECURITY.md tells you to report privately when unsure. Arguments for a public issue: the actor is an authenticated same-team API-token holder acting inside their own tenant, so it is a missing business rule rather than a privilege escalation, and the maintainers evidently take comparable integrity bugs publicly (#3188, #3189, #2475 'Ghost Recipients'). If the advisory route is chosen and the maintainers say it is out of scope, refile it as a public bug report; do not file both at once. Also note documenso runs OpenAI Codex security analysis over the codebase and warns it may close a report as a duplicate of a Codex finding. One legitimate exception the fix must preserve, already recorded in the review and worth repeating in the report: documentMeta.allowDictateNextSigner lets a signer nominate the next recipient on a PENDING envelope. **Re-evaluation:** The finding stands, unchanged and unweakened. Every one of the six culprit files is byte-identical between the tested v2.16.0 and today's v2.17.0, so the defect ships in the newest release exactly as reviewed: createEnvelopeRecipients gates only on assertEnvelopeMutable (a no-op for SES) plus envelope.completedAt, and update-envelope-recipients, delete-envelope-recipient, create-envelope-fields and update-envelope-fields carry the same two-gate shape. All three doc pages that promise the freeze are still live, one of them having merely moved line (documents.mdx:755 to :898). The review's fixedAfter claim ('still broken on main') is still correct; its only detail that has aged is the head sha it cites (688ef2fd, 2026-08-14) and the documents.mdx line number. Nothing in the 135 issues and PRs opened between 2026-08-01 and 2026-08-19 reports this: the two nearest, #3188 and #3189, are both v1 set-* endpoints and both are about collateral destruction of existing rows rather than the missing PENDING guard. Scope note for the filer: steps 8-12 of the scenario, the recipient update, recipient delete and field write refusals, still have no runtime evidence and are source-level inference only, so the issue body should either present them as 'the same two-gate shape, so we expect the same result' or be backed by the live re-run the plan schedules.

### D2a (Documenso) - POST /envelope/field/update-many moves nothing (route reads page/positionX/positionY, service writes pageNumber/pageX/pageY)

**Live re-run:** update-many with {id, type, positionY: 60} returns 200 with positionY "20", and the stored field is unchanged. A control call carrying page, positionX, positionY, width and height together also moves nothing, so the no-op is not confined to partial bodies. **Filer notes:** Two things a filer must get right. (1) #3136 fixes THIS half only; the sibling finding D2b (fieldMeta reset) survives the PR untouched, because the mapped object still passes `fieldMeta: field.fieldMeta`, the zod-defaulted value. Any comment on #3136 should say so explicitly, otherwise a maintainer merging it will believe update-many is fixed. (2) A comment on #3136 carries no reproduction record if the PR is later closed, so consider pairing it with a single bug-report issue that covers D2a and D2b together and links #3136 as the partial fix. The community PR is 21 days old with no maintainer comment (only vercel and coderabbit bots), so it may sit indefinitely. **Re-evaluation:** The finding stands exactly as written. Nothing has touched the service, the route or the route types since v2.16.0, and v2.17.0 (cut 2026-08-19) ships the same code, so the coordinate no-op is now live in two consecutive releases rather than one. One review claim is now WRONG and must not be repeated in the filing: the review says PR #3136 'baseRefName is docs/documents-cancel-get-many, not main, so it is stacked on another unmerged branch and cannot land on its own'. That base branch was merged on 2026-08-19 as #3135, and #3136 has been rebased onto main; it is now an ordinary open PR against main. Its code half is still the exact fix for this finding. Everything else holds: no upstream issue reports the no-op, and nothing in the 135 items opened between 2026-08-01 and 2026-08-19 duplicates it.

### D2b (Documenso) - update-many wipes the field's fieldMeta to type defaults on any partial update

**Live re-run:** The same 200 replaces {label:'Job Title', placeholder:'Enter your job title', required:true, characterLimit:40, textAlign:'left'} with the text-type defaults. Same run as D2a; the three relevant captures are copied into D2b/. **Filer notes:** File this as its own bug report even if D2a is handled by commenting on #3136, precisely because #3136 does not fix it and merging #3136 will hide the remaining symptom. The report should name the fix shape: give the update route a schema whose fieldMeta is optional with NO default, so an omitted fieldMeta leaves the column untouched (create keeps its defaults). Blast radius to state in the issue: all ten fieldMeta-bearing types; RADIO drops to a single blank option, DROPDOWN to a single 'Option 1', CHECKBOX to one blank unchecked option, TEXT and NUMBER lose label, placeholder, characterLimit and min/max. Also worth telling the maintainer: the docs' own update example, { id, type, pageY }, is exactly the call that triggers the wipe. **Re-evaluation:** The finding stands and its urgency has gone UP relative to the review. Nothing has changed in the schema or the write, so v2.17.0 ships the silent data loss unchanged. What changed is the state of #3136: it is now rebased onto main and therefore mergeable at any time, and merging it will make the endpoint LOOK fixed (moves start working) while the fieldMeta wipe continues silently, because the PR's new mapping still forwards the defaulted fieldMeta. That is a worse end state than today for anyone diagnosing the endpoint: the loud symptom disappears and the quiet one remains. No upstream item reports the reset, and nothing among the 135 items opened between 2026-08-01 and 2026-08-19 mentions it.

### D3 (Documenso) - An ASSISTANT recipient is accepted on a PARALLEL envelope

**Live re-run:** ASSISTANT accepted with 200 both on the default envelope (whose documentMeta.signingOrder reads PARALLEL) and on one set to PARALLEL explicitly. The created recipient carries signingOrder: null, the value that makes the assistant's own document view come back empty. **Filer notes:** Frame the issue as 'no refusal at all, the recipient is created', not as 'wrong status code': no doc page publishes a status for this refusal, and the scenario's expected 400 was the test's inference. Cross-link #2973 in the body and say plainly that there are two acceptable resolutions, enforce the documented rule server-side or change the docs and the workflow the way #2973 asks, because a maintainer reading this cold will otherwise think the two conflict. Two extra facts worth including because they raise the stakes beyond a contract violation: with signingOrder PARALLEL, send-document.ts notifies everyone at once so signers can act before the assistant, and getRecipientsForAssistant and getFieldsForToken filter on `signingOrder: { gte: assistant.signingOrder ?? 0 }`, which is NULL-false in SQL for the null signingOrder an API-created parallel recipient carries, so the assistant's own view comes back empty. Also state that a complete fix must cover update-envelope-recipients and the envelope-meta path, since flipping an envelope back to PARALLEL while an ASSISTANT exists reaches the same illegal state from the other side. **Re-evaluation:** The finding stands, unchanged in every respect. The culprit file is byte-identical between the tested tag and v2.17.0, the two doc pages that state the restriction are byte-identical too, and both cited upstream items are in exactly the state the review recorded (#2973 open and untouched since 2026-06-13, #2995 closed unmerged). Nothing among the 135 issues and PRs opened between 2026-08-01 and 2026-08-19 touches the assistant role or signing order, and a direct 'assistant' search over the whole tracker surfaces no report of the API accepting the combination. The review's fixedAfter claim ('live on main as of 2026-08-15') is still true one release later. No change to verdict, confidence or scope.

### D4 (Documenso) - POST .../reject returns 200, the next GET still says PENDING (status written by an async job)

**Live re-run:** Reject returns 200 with signingStatus REJECTED on the recipient; the envelope still reads PENDING at 12 ms and at 112 ms after that 200, and flips to REJECTED at 52 ms in one run and 638 ms in another. Window is shorter than the original run's; reframe the finding around the unenforced 'other pending recipients cannot act' promise rather than latency. **Filer notes:** The strongest framing for the maintainer is not the stale read, it is the second doc bullet: 'Other pending recipients can no longer act on the document' is unenforced for the whole window, because sign-field-with-token and complete-document-with-token gate only on envelope.status === PENDING plus the acting recipient's own status, while the cancellation emails have ALREADY gone out. That makes it a correctness gap, not a latency nit. Include the fix shape the review proposed: write Envelope.status = REJECTED and the DOCUMENT_REJECTED webhook inside the same transaction that flips the recipient, leaving the seal job to produce only the stamped PDF; that also removes the dependency of a terminal legal state on a PDF pipeline that demonstrably fails (#2921, #3092, #3191). A docs-side alternative exists if maintainers say the async design is intended: say in document-lifecycle.mdx that the status settles after sealing and point integrators at the DOCUMENT_REJECTED webhook. Mention in the issue that recipient-level truth IS immediate in the same payload, so an integration can read the rejection off the recipient array before the envelope agrees. **Re-evaluation:** The finding stands unchanged. Both culprit files are byte-identical between v2.16.0 and v2.17.0, so the rejection handler still commits only the recipient row and the audit log and leaves Envelope.status to the async seal job; the doc sentence promising an immediate move to Rejected is likewise byte-identical. #3191, the review's nearest neighbour, is still open with zero comments after six days, so the review's 'open and unanswered' remains literally true and the narrower TSP variant is still the only filed form of the root cause. Two new items sharpen the picture without duplicating it: #3180 reports a second missing guard on the same rejection path, and the closure of #3234 means the sweep cadence that partially mitigates this is still hardcoded to the 15-minute-to-6-hour window. Nothing changes the verdict, confidence or scope. One evidence limitation carried over from the review and still true: the transcript proves only that the flip had not happened within roughly 200 to 400 milliseconds; the source, not the run, is what establishes that the write is async.

### D5 (Documenso) - required: true + readOnly: true on a field is accepted by the API

**Live re-run:** All nine forbidden fieldMeta configurations return 200 and are persisted (TEXT required+readOnly, readOnly TEXT with no text, readOnly NUMBER with no value, readOnly RADIO with no options, readOnly CHECKBOX with nothing checked, readOnly DROPDOWN with no default, DROPDOWN default not among its options, NUMBER default above max, NUMBER default below min). The original run observed only the first. **Filer notes:** The issue should say the fix is already written elsewhere in the repo: createEnvelopeFields can call the same per-type validators that set-fields-for-document.ts:136-195 calls, and should throw AppError(AppErrorCode.INVALID_REQUEST) so the API answers 400, rather than the bare `new Error(errors.join(', '))` the setFields path throws today (which surfaces as a 500 even where the rules ARE enforced). Two scope facts to state: the same unvalidated service also backs the v1 POST /document/field/create-many, so this is not v2-only; and the doc states the rule as a product-wide invariant, repeating it in five separate Rules lists, so it cannot be dismissed as a stray sentence. Report only the observed TEXT case as observed and list the other eight as expected-but-unverified, or re-run them first if the live step covers it. **Re-evaluation:** The finding stands, unchanged. The create path, the shared validators it fails to call and the doc page that promises the rule are all byte-identical between the tested v2.16.0 and today's v2.17.0, so the gap now ships in two consecutive releases. Both cited PRs are still open and still only touch validateNumberField, confirming the review's reading that the community treats packages/lib/advanced-fields-validation as the home of these rules while the v2 create path bypasses it entirely. No new upstream item reports the gap. The review's caveat still applies and must survive into the filing: only the required+readOnly TEXT case was actually observed, because the run aborts at the first mismatch; the other eight forbidden configurations in the scenario are inference from 'no fieldMeta validation runs at all on this path', not evidence.

### D6 (Documenso) - Rate limit advertised by v2.16.0 is 100 req/min; docs say 1000 (fixed on main by PR #3081)

**Live re-run:** v2.17.0 answers x-ratelimit-limit: 1000 with x-ratelimit-remaining decrementing, both without and with DANGEROUS_BYPASS_RATE_LIMITS. PR #3081 is now in a release and #3133 removed the contradictory docs callout, so neither half survives. Do not file. **Filer notes:** Worth keeping one sentence of this in whatever public write-up the batch produces, because the underlying pattern is not fixed: docs.documenso.com is built from main, so any doc change describing new behavior is published before the release that ships it, and a self-hoster installing a tag cannot tell from the docs which build they describe. That is a process observation, not a bug report, and it should NOT be filed as an issue on its own. Also note for the live step: v2.17.0 is both the newest tag and today's main head, so testing main and testing the release are the same thing today. **Re-evaluation:** This finding is now closed by upstream and should not be filed. At the review it was a genuine release-lag defect: #3081 merged 52 minutes after v2.16.0 was tagged, so every tagged build in existence enforced 100 while docs.documenso.com, built from main, published 1000. v2.17.0 (2026-08-19) contains #3081, so the newest release now advertises x-ratelimit-limit: 1000 and matches the docs. The review's fixedAfter block said 'Fixed on main, in no tag'; that is now out of date, the correct statement is 'fixed and released in v2.17.0'. The secondary doc defect the review recorded in its notes, the contradictory 'No rate limit headers are currently provided' callout, was also resolved on 2026-08-19 when #3133 merged, so neither half of this finding survives. The live re-run the plan schedules against v2.17.0 should confirm 1000 on the wire; if it reports 100 the assumption to re-examine is which build was started, not this analysis.

### D7 (Documenso) - After a distribution-method None send, the document page shows no signing link

**Live re-run:** After a distribution-method None send the document page carries no /sign/ in its text, in any anchor href, or in any input value. The redirect carries ?action=copy-links, captured live via a history.pushState hook. The 'Copy Signing Links' dialog behind the unnamed three-dots menu does not display the link either, only a Copy button, which supports the docs-side resolution as well as the code-side one. **Filer notes:** File D7 and D9 as ONE issue or as two? Recommend one issue for D7 with D9 mentioned as the aggravating factor, and a separate D9 issue for the app-wide naming pattern, because the two have different fixes and different audiences. The D7 body should lead with the concrete mechanism, not the symptom: the product already has DocumentRecipientLinkCopyDialog mounted on this very page, auto-opening on '?action=view-signing-links', and the template flow (documented with the identical sentence) does get it, while the send flow redirects with '?action=copy-links' and gets only a pulse plus a tooltip on the first recipient. That single sentence makes the fix obvious and cheap. Include the secondary observation that the tooltip reaches only recipient 0 while every row pulses, so on a multi-recipient None send the other rows pulse with no explanation. Screenshot from the run (step-15.png) is available and the bug-report template has a Screenshots field. Also flag the stale identifier drift (comment says view-tokens, variable is hasViewTokensAction, value compared is copy-links, sibling uses view-signing-links) as the likely reason the two paths diverged. **Re-evaluation:** The finding stands with its original verdict and its original medium confidence, and nothing has changed on either side. Both culprit files, both supporting files and both doc pages are byte-identical between the tested v2.16.0 and today's v2.17.0, so the send path still redirects with the action value that only pulses the copy icons while the mounted dialog listens for a different one. No upstream item reports it. The confidence stays at medium for the reason the review gave, not for a new reason: the finding straddles code and docs, because even the link dialog renders only email plus a Copy button, so 'signing links are displayed' is not literally true anywhere in the product, and a maintainer could legitimately resolve this by rewording send.mdx and templates/use.mdx instead of changing the redirect. The filer should present both resolutions rather than assert the code is wrong.

### D8 (Documenso) - The API-token expiry chooser has no accessible name

**Live re-run:** getByRole('combobox', {name:'Expires in'}).count() = 0; aria snapshot reads 'combobox: 3 months' with no name; the visible label's htmlFor '_r_f_-form-item' resolves to no element. Clicking positionally reveals the six options as 7 days, 1 month, 3 months, 6 months, 12 months, Never, so the previously unobserved '12 months' vs docs '1 year' mismatch is now recorded. **Filer notes:** This is really two reports in one and the filer should keep them separate inside a single issue, or split them. (a) Accessibility: the expiry chooser has no accessible name because FormControl's Radix Slot applies id={formItemId} to <Select>, which is SelectPrimitive.Root, a non-DOM provider, so the label's htmlFor dangles, and role=combobox is not a name-from-content role so the value text '3 months' does not name it either. The Name field in the SAME dialog proves the Slot works when the child is a real DOM element, which is the cleanest possible demonstration for a maintainer. Quantify it as the review did: 46 FormControl-wrapping-Select sites and 1 aria-label across 84 SelectTriggers. The shadcn-convention fix is to move FormControl to wrap SelectTrigger instead of Select. (b) Wording: the UI labels the longest period '12 months', the doc says '1 year'; the durations are identical (ONE_YEAR resolves to Duration.fromObject({ years: 1 })), so this is a one-word fix on whichever side the maintainers prefer, and the docs live in this same repo under apps/docs/. Because (b) is a documentation matter, mention 'documentation' and the apps/docs path in the body so a triager can apply the type: documentation label; there is no docs-specific issue template. **Re-evaluation:** The finding stands unchanged, and one plausible way it could have been fixed incidentally has been ruled out: the react 19 migration (#3107) landed in this window and did not touch form.tsx, select.tsx or the dialog, all three of which are byte-identical to the tested tree. So v2.17.0 ships the unnamed combobox exactly as reviewed. The docs sentence survived a commit to its file (#3022) and still reads '1 year' while the UI still says '12 months'. No upstream report exists: an 'accessibility' search over the whole tracker returns 25 results with not one about an accessible name or aria-label, matching the review's 'unreported'. The review's own framing caveat must survive into the filing and is the main thing a filer can get wrong: the observed failure is an accessibility defect, NOT a violation of the bound doc sentence, because the scenario never reached the six options; the '12 months' versus '1 year' wording mismatch is real but was never exercised.

### D9 (Documenso) - The Copy Signing Links control and the three-dots trigger have no accessible name

**Live re-run:** Converted from source reading to observation. The recipient-row CopyTextButton and the three-dots trigger (data-testid document-page-view-action-btn) both snapshot as a bare '- button'; 5 of the 9 buttons on the page have an empty accessible name. Inside the dialog the same copy action does get the name 'Copy', which is a clean in-product contrast for a maintainer. **Filer notes:** Two controls, one issue. (1) CopyTextButton (packages/ui/components/common/copy-text-button.tsx) is icon-only with no aria-label; lucide-react emits a bare <svg> with no <title>, so the accessible name is empty, and the Radix TooltipContent that reads 'Copy Signing Links' is aria-describedby on the wrapping div, a description not a name, and is only force-open for the first recipient. (2) The document page's three-dots DropdownMenuTrigger (document-page-view-dropdown.tsx:73) has only data-testid, which is not an accessible name, and it is the only textually named route to the 'Signing Links' item. Both are one-line fixes (add aria-label, or sr-only text inside the button). Pair this with D7 in the body: the reason the naming gap matters more here than elsewhere is that after a distribution-method None send these two unnamed controls are the ONLY routes to the promised signing links. Do not open a PR even though the fix is trivial. **Re-evaluation:** The finding stands unchanged. All three culprit files are byte-identical between v2.16.0 and v2.17.0, so both unnamed controls ship in the newest release, and the react 19 migration that landed in this window did not touch them. No upstream item reports an accessible-name defect anywhere in the app. One scope caveat inherited from the review and still true: this finding was confirmed IN SOURCE, not executed, because scenario step 16 never ran (step 15, which is D7, failed first). The evidence is therefore a code reading plus the run's page-text observation that 'Copy Signing Links' appears twice as tooltip text rather than as a control label. If the live re-run reaches step 16 it will convert this to observed; until then the issue body should not claim a screen-reader test was performed.

### D10 (Documenso) - The documented v2 error format {error, code, statusCode} is never emitted; top-level INTERNAL_SERVER_ERROR rider

**Live re-run:** 404, 400 and 401 bodies captured. None carries a top-level error or statusCode. The AppError-derived 404 and 401 both carry top-level code INTERNAL_SERVER_ERROR; the zod 400 correctly carries BAD_REQUEST. The 401 shows statusCode does exist, nested at data.appError.statusCode. **Filer notes:** Two things for the filer. (1) Scope the issue to first-api-call.mdx only and cite the merged #3133 as the shape of the correction the maintainers already accepted; do not claim rate-limits.mdx is still wrong, it was fixed today. (2) The finding has two halves and only the first is a pure doc bug: the missing `error`/`statusCode` keys (doc fix) and the top-level INTERNAL_SERVER_ERROR on AppError-derived failures (product fix, one line, use genericErrorCodeToTrpcErrorCodeMap[...].code which is already computed and discarded at trpc.ts:59 and hono-trpc-open-api.ts:27). Open PR #3219 is refactoring exactly that function right now, which is a natural place to mention the second half. Also worth repeating from the review: /api/v2 genuinely serves two envelopes, since the hand-rolled Hono download routes do return {error, code}, so an issue that says 'the API never returns `error`' would be overstated. **Re-evaluation:** The finding stands unchanged and is now stronger. Neither half moved: the doc block is byte-identical (two lines lower) and still live at docs.documenso.com, and the product still cannot emit `error` or `statusCode` on any tRPC-backed v2 route. What changed is the surrounding evidence. PR #3133 merged today, so Documenso itself has now published the real v2 error shape on rate-limits.mdx, which makes first-api-call.mdx the last page carrying the invented envelope and makes the internal contradiction the review described a matter of record rather than of a pending PR. Critically, no PR in the merged or open stack touches this block: I read the first-api-call.mdx hunks of #3137 (it changes the 429 row at L441 and the rate-limit prose at L485 and steps over L447-L484 untouched) and the file lists of #3138 and #3139 (neither includes first-api-call.mdx). So the review's fixedAfter: no is still correct, and the only correction the review hedged on (partial credit via #3133) has now landed for the sibling page only. The INTERNAL_SERVER_ERROR rider is also unchanged: #3219 rewrites the errorFormatter without changing the shape, and the dead `code` half of genericErrorCodeToTrpcErrorCodeMap gained two more rows nobody reads.

### D11 (Documenso) - Field coordinates documented as number in responses; the wire sends JSON strings

**Live re-run:** Coordinates sent as JSON numbers come back as strings: positionX "10", positionY "80", width "20", height "5"; page stays a number 1. This is the wire capture PR #3136's reviewer asked for. **Filer notes:** The filer should note that #3136 is a two-headed PR: its code half fixes D2a (the update-many coordinate drop) and its docs half fixes both D11 and D12. A comment should confirm the string coordinates with an actual wire capture (the scenario's data[0] response), since that is the one thing dguyen asked for and the author answered only from source. Two doc gaps #3136 does not close, worth listing in the same comment: the unquoted coordinates in response examples on documents.mdx, first-api-call.mdx and examples/common-workflows.mdx, and the undocumented fieldMeta.overflow default plus the documentId/templateId compat keys the Field Object table omits. **Re-evaluation:** The finding stands verbatim: nothing on either side has moved, the doc still types the coordinates `number` at the same line numbers and the API still answers JSON strings. The only change is upstream momentum. #3136 is no longer stacked on a non-main base (the review's third refinement of the colleague finding is now out of date, and the filer must not repeat it), and the single maintainer doubt about the string typing was answered by the PR author today with the same Prisma Decimal reasoning the review reached independently. That leaves this finding fully covered by a live, maintainer-approved-adjacent PR whose siblings merged hours ago, so the cheapest correct action is to strengthen that PR, not to open a competing issue. Confidence and verdict (doc-bug, not a product defect) are unchanged.

### D12 (Documenso) - The Fields guide's create/update bodies and response key are the legacy /document/field/* shape

**Filer notes:** D11 and D12 are the same PR and should be handled in one comment. The comment's added value over the PR text is the reproduction: the exact 400 body {"issues":[{"path":["envelopeId"],"message":"Required"},{"path":["data"],"message":"Required"}]} from posting the page's own curl, plus the observation the PR does not state, that the documented body is not invented but is the live contract of the still mounted legacy POST /api/v2/document/field/create-many, so a reader who found the page plausible was not being careless. If the maintainers prefer, the alternative one-line fix is to repoint the samples at the legacy URLs. **Re-evaluation:** The finding stands with no change to verdict, confidence or scope: fields.mdx is untouched since the tested tag, so every published create and update example on that page still 400s as printed, and both contracts it confuses are still live. The only material update is that the fix PR #3136 is now based on main rather than on a sibling docs branch, which removes the review's 'cannot land until the whole stack is reviewed' obstacle; three PRs from that same stack merged into v2.17.0 this morning, so this one is plausibly next. The review's upstream claims all still hold, with that one correction about the base branch.

### D13 (Documenso) - POST /envelope/get-many is documented as taking {envelopeIds}; it takes {ids: {type, ids}}

**Live re-run:** Product contract unchanged: the documented {envelopeIds} body still returns 400 with issue path ['ids']. The docs shipped in v2.17.0 now publish the real contract {ids:{type,ids}} with the 1-20 cap and the documentId/templateId variants; envelopeIds no longer appears in documents.mdx. Do not file. **Filer notes:** Worth carrying forward for the report rather than for a filing: the two-week 'community docs PRs sit unmerged' narrative needs updating for Documenso, since three of the seven merged on 2026-08-19 (#3133, #3134, #3135), all approved by dguyen on 2026-08-03 and merged 16 days later. The remaining four (#3136, #3137, #3138, #3139) are still open. Also note #3135 shipped more than this finding: a new Cancel Document section, a CANCELLED status row, removal of the nonexistent source: "API" enum value, and the pagination corrections that D14 partly depends on. **Re-evaluation:** This finding is fixed upstream and should not be filed. The review's fixedAfter: no was correct on 2026-08-15 and is wrong as of today: PR #3135 was rebased from its docs/envelope-openapi-descriptions base onto main and merged at 2026-08-19 09:28:55 UTC as d42254ff5, which sits inside tag v2.17.0 cut the same day, and the published page already serves the corrected body. The review's characterisation of the defect was exactly right and its recommended fix is what shipped, including the nested selector, the 1-20 cap, the numeric documentId/templateId variants and the previously absent response section. The product contract was never at fault and is unchanged. Nothing about the verdict changes, only its disposition: from unreported-and-open to fixed-in-release.

### D14 (Documenso) - List responses documented with pagination: {page, perPage, totalPages, totalItems}; the counters are flat

**Live re-run:** Scope reduced. The wire has flat counters data/count/currentPage/perPage/totalPages and no pagination object. documents.mdx (the bound page) was corrected by #3135 in v2.17.0, but first-api-call.mdx L81-86 still publishes pagination: {page, perPage, totalPages, totalItems}, as do templates.mdx, teams.mdx and common-workflows.mdx. **Filer notes:** The filer must not describe this finding as 'documents.mdx is wrong' any more; that half shipped in v2.17.0. What remains is first-api-call.mdx (#3137), templates.mdx and teams.mdx (#3138) and common-workflows.mdx (#3139). #3137 is the right place to comment because it carries the remaining bound page, but note that it now has a dirty merge state and no maintainer has ever reviewed it, so the comment should say plainly that its base merged and it needs rebasing onto main. If the maintainers would rather have an issue tracking the four leftover pages as one item, that is a reasonable fallback; the rename map to state is page -> currentPage, totalItems -> count, with perPage and totalPages keeping their names and only losing the nesting level. **Re-evaluation:** The finding stands but its scope has shrunk by one page, and the review's fixedAfter: no is now only half true. PR #3135 merged this morning into v2.17.0 and removed the fabricated pagination wrapper from documents.mdx, the file the review named as rootCause, replacing it with the real flat counters in both the response example and the paging loop. The second bound page, first-api-call.mdx, is untouched and still publishes "pagination": { page, perPage, totalPages, totalItems } at L81-L86, as do templates.mdx, teams.mdx and common-workflows.mdx, and I confirmed that live on docs.documenso.com today. So the scenario would still fail if it were bound to the getting-started page, and a developer reading Example 1 there still gets a TypeError. The product side is entirely unchanged and remains correct against its own schema, so this is still a pure doc bug with a docs-only fix. The three remaining corrective PRs are all still open and #3137 is now 'dirty' and needs a rebase after its base moved.

### D15 (Documenso) - Docs say a token without access gets 403; cross-tenant reads answer 404 by design

**Live re-run:** Outsider token on the owner's envelope answers 404 'Envelope could not be found' where the authentication page documents 403; a control call with the owner token returns 200 on the same envelope, so the 404 is the deliberate anti-enumeration answer rather than a lookup miss. **Filer notes:** Two filer notes. (1) Frame it as a documentation correction, not a security report: cross-tenant 404 is the deliberate anti-enumeration design, the function even carries the 'Needs at minimum two reviewers' warning, so the ask is to change the accordion to 404 and to scope the common-errors FORBIDDEN row to licence-gated and CSC features. (2) The strongest supporting evidence is Documenso's own per-endpoint status tables, which list 400/401/404/429/500 and never 403 (fields.mdx, recipients.mdx, first-api-call.mdx) while the error-handling switch in examples/common-workflows.mdx has no 403 branch. Consider raising it inline on open PR #3139 as well, since it is already rewriting that accordion. **Re-evaluation:** The finding stands exactly as written, on both sides, with only a two-line drift in the doc (L194 -> L196, caused by the pre-review EnvelopeWarning banner). Nothing new was filed. One thing the review could not have known: open PR #3139, 'docs: fix workflows, authentication, error codes and id formats', edits this very accordion, and it does not fix it. It rewrites the body sentence from 'Ensure you're accessing resources owned by the token's account' to '...the token's team' while leaving the '403 Forbidden' title, and its common-errors.mdx hunk keeps the FORBIDDEN row and adds CSC_UNLICENSED (403) alongside. So a knowledgeable contributor has been over these exact lines twice without noticing that the API answers 404 there, which raises rather than lowers the value of reporting it. Verdict (doc-bug, low impact, no data exposure) and confidence are unchanged.

### D16 (Documenso) - Docs say clicking a document opens the editor; it opens the read-only page

**Filer notes:** A one-sentence patch would close this faster than an issue, and the repo takes community docs PRs (four merged or open from one contributor this month), so if the filer is willing to send a PR rather than an issue that is likely the shortest path. Whichever route, the fix should cover both lines of the same step, since 'In the Recipients section, click + Add Signer' is unreachable until the navigation sentence is corrected, and sibling pages under docs/users/documents should be grepped for the same phrasing before filing. **Re-evaluation:** The finding stands with nothing changed at all: no commit has touched the doc page or the title-link component since the tested tag, no PR in the community docs stack goes near apps/docs/content/docs/users (the stack is entirely under docs/developers plus four trpc types files), and no new issue mentions it. It remains the smallest of the Documenso findings, a single sentence that describes navigation the product has never had, with the correction already visible in the product (the row's Edit action and the document page's Edit button). Verdict, confidence and impact are all unchanged.

### D17 (Documenso) - Download version=signed on a pending envelope (test defect; filename note only)

**Filer notes:** If anyone later wants to file the filename note, it belongs as a comment on existing issue #3096, which is already about the Content-Disposition filename built by the same handler, not as a new issue. **Re-evaluation:** Not a finding, and re-verification does not change that. The review's verdict was test-defect: the scenario asserts both that version=signed on a PENDING envelope returns a PDF (step 7) and that it does not (step 8), so one step fails by construction, and the product did what both bound doc pages say it does. Today the download handlers are untouched since the tested tag and both doc sentences are still in place, so there is nothing to report and nothing to re-evaluate. The right action remains a corpus-side conflict resolution in favour of common-workflows.mdx, not an upstream filing. The one genuine observation in the review, that Content-Disposition still names the unsigned original <title>_signed.pdf, is a minor naming wart the docs already warn about and is not worth an issue on its own.

### C1 (Cal.diy) - An out-of-office period is ignored when availability comes from date overrides

**Filer notes:** Two scenarios back this finding and only one of them is evidence. an-out-of-office-period-closes-the-booking-page.web.1 is the real-defect verdict; out-of-office-notes-shown-publicly-reach-the-booker.web.1 was ruled `environment` (the page never reached DOMContentLoaded inside the harness's 10 s budget), so it proves nothing and must not be cited as a second observation. The public-notes half of the doc claim is untested. The doc claim is a cal.com help-centre page (help/availabilities/out-of-office.md), i.e. it documents the commercial product, but the OOO feature and its whole render path ship in Cal.diy, so the issue belongs on calcom/cal.diy. Worth telling the maintainer: calculateOutOfOfficeRanges has no unit test anywhere in the repo, and the only OOO coverage (date-ranges.test.ts:548) hands buildDateRanges a pre-built outOfOffice map and so drives straight past the buggy filter. calcom/cal.com and calcom/cal.diy are the SAME repository: gh api repos/calcom/cal.com returns full_name calcom/cal.diy (the repo was renamed, GitHub redirects the old path). Every calcom/cal.com#NNNN reference in the review resolves to the same number on calcom/cal.diy, and one `--owner calcom` search covers both. The commercial Cal.com source is private, so nothing about the hosted product is verifiable and no "fixed in cal.com" claim can be made. **Re-evaluation:** The finding stands unchanged. Cal.diy main has not moved since 2026-08-08 and none of its 4 post-tested commits touches getUserAvailability.ts, date-ranges.ts, slots.ts or DatePicker.tsx, so the flattenDays guard that drops every OOO day when the user's availability rows are date overrides (empty `days` arrays) is still there verbatim at lines 744-755, and so is the copy at lines 860-870 that gives the Holidays feature the same hole. The review's fixedAfter claim ("not fixed") is still correct and its two searches were genuinely empty, not a tooling artefact: my org-wide sweep for OOO items updated since 2026-08-01 found only one new item, #29951, which is a different OOO defect in the same function. Verdict and scope are unchanged; confidence stays medium for the reason the review already gave, not for anything new: the seeded OutOfOfficeEntry row and the date-override-only schedule were never read back (the seed script is absent and the sandbox database is gone), so the trigger is inferred from the run's auto-selected-day pattern rather than observed. That is exactly what the planned live reproduction settles, and it is a cheap one: one user, one availability row that is a date override on day D, one OOO entry covering D, then GET the slots for D.

### C2 (Cal.diy) - GET /v2/slots?usernames=a,b&format=range returns every slot with end === start

**Filer notes:** The doc claim is cal.com's published v2 API reference (get-available-time-slots-for-an-event-type), which Cal.diy has no substitute for, but the faulty code is Cal.diy's own, so the issue goes to calcom/cal.diy. Two adjacent smells to include in the same report, both still present at head: the `if (!start || !end)` guard at line 155 cannot catch a missing duration because luxon turns plus({minutes: undefined}) into a no-op rather than an Invalid DateTime, and getAvailableRangeSlotSeated is declared with eventTypeSeatsPerTimeslot: number yet called with `eventType.seatsPerTimeSlot ?? undefined` at lines 148 and 166. Steps 2 and 3 of the scenario never ran, so the doc's "usernames must contain at least 2" and "organizationSlug is required" claims are untested and must not be asserted. calcom/cal.com and calcom/cal.diy are the SAME repository: gh api repos/calcom/cal.com returns full_name calcom/cal.diy (the repo was renamed, GitHub redirects the old path). Every calcom/cal.com#NNNN reference in the review resolves to the same number on calcom/cal.diy, and one `--owner calcom` search covers both. The commercial Cal.com source is private, so nothing about the hosted product is verifiable and no "fixed in cal.com" claim can be made. **Re-evaluation:** Stands, unchanged and still high confidence. No commit since the tested tree touches apps/api/v2/src/modules/slots/ or packages/features/eventtypes/lib/defaultEvents.ts, so the dynamic pseudo-event still has id 0, getEventTypeById(0) still misses, slotDuration is still undefined and luxon's plus({minutes: undefined}) still yields end === start behind an HTTP 200. Nothing new in the tracker: the two searches cover the exact query shape (format=range) and all slot activity since 2026-08-01, and neither surfaces this symptom or anything that would fix it. The review's upstream note that its three searches were exhaustive holds. Scope is unchanged and worth restating in the issue because it is what makes the report actionable: only the dynamic form (usernames=a,b, no eventTypeId) with format=range is affected; a real event type with format=range is correct (proven by the passing sibling scenario ask-for-a-particular-slot-length.api.1), and sending &duration=N works around it.

### C3 (Cal.diy) - POST /v2/bookings/{uid}/attendees on a seated booking returns 201 and attaches a seat-less attendee

**Filer notes:** Only the first half of the finding is evidenced by the run: the add is accepted (201, attendee id 79 on booking 73, event type 102 with seatsPerTimeSlot 10). The seat-count drift (display counts raw Attendee rows, the seat gate counts only rows with a BookingSeat) is derived from source, not observed, because the scenario aborted at step 2 and nothing re-read the booking. The FINDINGS-INDEX liveNote asks for exactly that re-read; until it is done the issue should claim the 201 and describe the seat arithmetic as a code-level consequence. The guests endpoint (booking-guests.service.ts) has the identical hole and its documented refusal is equally false, so file both in one issue. calcom/cal.com and calcom/cal.diy are the SAME repository: gh api repos/calcom/cal.com returns full_name calcom/cal.diy (the repo was renamed, GitHub redirects the old path). Every calcom/cal.com#NNNN reference in the review resolves to the same number on calcom/cal.diy, and one `--owner calcom` search covers both. The commercial Cal.com source is private, so nothing about the hosted product is verifiable and no "fixed in cal.com" claim can be made. **Re-evaluation:** Stands unchanged. Neither booking-attendees.service.ts (v2), packages/features/bookings/services/BookingAttendeesService.ts nor booking-guests.service.ts was touched by any of the 4 commits since the tested tree, so POST /v2/bookings/{uid}/attendees on a seated booking still returns 201 and still creates an Attendee row with no BookingSeat. No new tracker item: nothing seated-related has been updated org-wide since 2026-08-01 and the documented refusal sentence appears nowhere in the tracker. The review's cited PR #27759 is unchanged (merged 2026-02-17). One framing point survives re-check and must be in the filed issue: the promise "Attempting to add an attendee to a seated event booking will return a 400 error" exists only in cal.com's published OpenAPI; the Cal.diy controller's own @ApiOperation has no Seated Events paragraph, so this is most likely a guard added on the commercial side after the April 2026 fork and never ported. That does not make it an edition mismatch, because the harm (a seat-less attendee that violates Cal.diy's own required seatUid in GetSeatedBookingOutput and splits its two seat counters) is entirely inside Cal.diy code.

### C4 (Cal.diy) - PATCH /v2/slots/reservations/{uid} accepts reservationDuration from an anonymous caller; GET/PATCH/DELETE unguarded

**Filer notes:** Report privately first: https://github.com/calcom/cal.diy/security/advisories/new (private reporting is enabled) and cc security@cal.com. Two policy frictions the filer should know. (1) SECURITY.md still reads as a hosted-service policy ("our clients", "our infrastructure or dashboard") and its contact is the commercial company's address, on a repo whose own bot says changes here are never deployed to Cal.com, so the report may land with a team that does not own the fork; say plainly in the first line that this is a self-hosted Cal.diy code report. (2) The out-of-scope bullet "Any activity that could lead to the disruption of our service (DoS)" will be quoted back if the report leads with "availability denial by pinning a slot for an unbounded number of minutes". Lead instead with the missing authorization: an anonymous caller changes state on a resource it does not own, and a documented authenticated-only field is accepted from an anonymous caller. One caveat on the scenario as encoded: it expects the anonymous PATCH to silently fall back to 5 minutes, but the correct fix (401, mirroring POST) would also make that step red; the assertion is directionally right, the expected shape is a guess, so quote the observed 200 + reservationDuration 30 rather than the scenario's expectation. calcom/cal.com and calcom/cal.diy are the SAME repository: gh api repos/calcom/cal.com returns full_name calcom/cal.diy (the repo was renamed, GitHub redirects the old path). Every calcom/cal.com#NNNN reference in the review resolves to the same number on calcom/cal.diy, and one `--owner calcom` search covers both. The commercial Cal.com source is private, so nothing about the hosted product is verifiable and no "fixed in cal.com" claim can be made. **Re-evaluation:** Stands, unchanged, and the security framing gets sharper on re-check. Nothing under apps/api/v2/src/modules/slots/ or packages/platform/types/slots/ has changed, so PATCH /v2/slots/reservations/{uid} is still guardless while its sibling POST enforces the same documented rule with 401 and 403, and GET and DELETE on the same uid are still unauthenticated with no ownership check. No one has reported it since the review. The review's careful narrowing still holds and the filer must keep it: the docs do NOT promise authentication for the PATCH, GET or DELETE endpoints (all three carry security: [] and the upstream e2e suite deliberately exercises anonymous PATCH and DELETE because the anonymous booker flow needs them); what they promise is that the reservationDuration FIELD is "ONLY for authenticated requests with api key, access token or OAuth credentials", and that rule is entirely unenforced on PATCH. So the doc-backed half is the field, and the rest (read, move to another eventTypeId, extend, delete another booker's hold by uid, with no upper bound on the duration) is an unpublished IDOR that is real in code but violates no sentence. The one thing the live step must settle is whether anonymous GET and DELETE really answer (the run only exercised PATCH); the FINDINGS-INDEX liveNote already asks for it.

### C5 (Cal.diy) - Rescheduling a PENDING booking with rescheduledBy = the attendee returns accepted

**Filer notes:** The doc sentence is not hosted-only: it ships in the Cal.diy tree as the DTO's own swagger description (packages/platform/types/bookings/2024-08-13/inputs/reschedule-booking.input.ts, RESCHEDULED_BY_DOCS), which makes this Cal.diy's own contract and removes any edition-mismatch defence. Blame is a regression worth naming in the issue: PR #19833 (2025-03-11) deliberately REMOVED the api-key-owner fallback so the owner would stop deciding confirmation, and PR #24640 (2025-10-30, an org-admin seated-booking fix) restored it as a one-line `?? authUser?.id`. Suggested fix shape: make input.service.ts return a tri-state instead of encoding "attendee is rescheduling" as an undefined that `??` cannot distinguish from "not supplied". If the maintainer keeps the current precedence, the docs and RESCHEDULED_BY_DOCS need a caller-identity sentence, because today they state an unconditional rule the code does not implement. calcom/cal.com and calcom/cal.diy are the SAME repository: gh api repos/calcom/cal.com returns full_name calcom/cal.diy (the repo was renamed, GitHub redirects the old path). Every calcom/cal.com#NNNN reference in the review resolves to the same number on calcom/cal.diy, and one `--owner calcom` search covers both. The commercial Cal.com source is private, so nothing about the hosted product is verifiable and no "fixed in cal.com" claim can be made. **Re-evaluation:** Stands unchanged, high confidence. None of the culprit files moved, so bookings.service.ts:769 still overrides the rescheduledBy-derived identity with the authenticated API-key owner and a PENDING booking rescheduled by an owner-key caller who correctly declares the attendee in rescheduledBy still comes back accepted. The tracker is unchanged: all three cited items are in exactly the state the review recorded, and no new item describes this symptom. The strength of the finding is that the evidence contains its own control, step 2 (outsider key, same request shape, pending) against step 7 (owner key, accepted), which isolates the caller credential as the deciding input. One thing the filer must NOT assert as observed: the second-order path where an UNauthenticated caller passes the host's email in rescheduledBy and gets the booking auto-confirmed. That is derived from input.service.ts:544-548 (findByEmail with no check that the caller is that person) plus OptionalApiAuthGuard returning null instead of rejecting; it was never run. If the live step confirms it, that half is a privilege bypass and should go the security route separately, while the main finding stays a public contract bug.

### C6 (Cal.diy) - Rescheduling a declined booking answers 404 Could not find original booking instead of 400

**Filer notes:** Two independent fixes belong in one issue: extend canRescheduleBooking to REJECTED and AWAITING_HOST with a 400 in the same style as the two cancelled branches, and route @calcom/lib HttpError through the v2 error envelope (a @Catch(HttpError) filter alongside the five already registered in apps/api/v2/src/bootstrap.ts) so no v2 route can answer with a raw {statusCode,message} body. awaiting_host has the identical hole and was never exercised because instant meetings were not seeded, so state it as code-derived. Do not claim anything about what commercial Cal.com returns for a rejected booking: that cannot be checked from source. calcom/cal.com and calcom/cal.diy are the SAME repository: gh api repos/calcom/cal.com returns full_name calcom/cal.diy (the repo was renamed, GitHub redirects the old path). Every calcom/cal.com#NNNN reference in the review resolves to the same number on calcom/cal.diy, and one `--owner calcom` search covers both. The commercial Cal.com source is private, so nothing about the hosted product is verifiable and no "fixed in cal.com" claim can be made. **Re-evaluation:** Stands unchanged, high confidence, and it is the cleanest of the eight to file because the run proves the contrast itself: steps 3 and 6 got the documented 400 for both cancelled arms, and step 9 got a 404 "Could not find original booking" for the rejected arm on a booking the same API had returned 45 ms earlier. None of the three culprit files changed, so 2 of the 5 documented non-reschedulable statuses (rejected, awaiting_host) are still uncovered by canRescheduleBooking and still fall through to a repository query whose status filter omits them. Nothing new in the tracker; both searches reproduce the review's results exactly. The finding is also correct independently of any doc version question (the bound doc page is generated from the commercial 2026-02-25 controller, which Cal.diy does not ship, but the scenario sent 2024-08-13, a version Cal.diy really has, so no silent downgrade occurred), because a 404 saying a booking cannot be found is a false statement about a booking that demonstrably exists.

### C7 (Cal.diy) - A slot reservation is not reflected in the very next GET /v2/slots (2 s cache)

**Filer notes:** Reproduction is two GET /v2/slots calls with identical query input inside 2 s straddling a POST /v2/slots/reservations; the second returns the pre-reservation body. Include the point that upstream's own e2e already encodes the contract this violates (apps/api/v2/.../e2e/user-event-type-slots.controller.e2e-spec.ts:431 "should reserve a slot and it should not appear in available slots" reserves and re-reads with no wait) and only passes because the preceding queries in that suite are older than the TTL or use different params, which makes the coverage timing-dependent. Fix options to offer: delete the cache keys touched by a reservation write, or fold the reservation state and the caller identity into the key. calcom/cal.com and calcom/cal.diy are the SAME repository: gh api repos/calcom/cal.com returns full_name calcom/cal.diy (the repo was renamed, GitHub redirects the old path). Every calcom/cal.com#NNNN reference in the review resolves to the same number on calcom/cal.diy, and one `--owner calcom` search covers both. The commercial Cal.com source is private, so nothing about the hosted product is verifiable and no "fixed in cal.com" claim can be made. **Re-evaluation:** Stands unchanged, high confidence. withSlotsCache is byte-identical at head and still uncoupled from the reservation write path, so for up to SLOTS_CACHE_TTL (2000 ms default) after a successful POST /v2/slots/reservations the next GET /v2/slots with the same query input still offers the held slot, and symmetrically a released hold stays hidden for the same window. Nothing new in the tracker; the org-wide slots sweep for items updated since 2026-08-01 also turned up nothing about the cache. Two scope points the review already established and re-check does not disturb: the harm is bounded, because a second reservation POST for the same slot is still refused with 422 (the reserve path reads the database directly), so this is a wrong slot grid rather than a double booking; and the cache cannot be switched off in a real deployment, since the v2 API halts without REDIS_URL. The secondary observation is worth filing alongside and is arguably worse: the key omits the request context entirely while _getAvailableSlots personalises the result by ctx.req.cookies.uid, so on the trpc/web path one booker's payload (which deliberately still shows that booker's own hold) can be served to a different booker inside the TTL.

### C8 (Cal.diy) - Both round-robin reassign endpoints return 200 success with a reassignedTo object and do nothing (fork stubs)

**Filer notes:** This is a Cal.diy-only defect created by the fork commit ab21c7f805 (PR #28903 "refactor: Cal.diy"), so it must be filed on calcom/cal.diy and must NOT be phrased as a cal.com bug: the commercial product still ships the real round-robin engine. The one-line ask is to make the two stubs throw like their four siblings (or unregister the routes / answer 501), never to return success. Of the two scenarios behind this finding only reassignment-refuses-a-non-round-robin-booking.api.1 carries it; the sibling reassign-a-round-robin-booking.api.1 was ruled test-defect (it asserted data.uid and data.hosts[0].id, fields no schema on either doc page defines) and must not be cited as evidence. That sibling did surface a separate, genuine DOC defect worth a second issue: the NestJS envelope class and the platform-types data class are both named ReassignBookingOutput_2024_08_13, so Nest's Swagger plugin collapses them and the published OpenAPI $refs the envelope from its own data property, leaving the reassign pages documenting zero response fields, and the prose still names ReassignAutoBookingOutput / ReassignManualBookingOutput, which exist nowhere in the codebase. Step 3 (the auto route) never ran, but roundRobinReassignment is the identical no-op. calcom/cal.com and calcom/cal.diy are the SAME repository: gh api repos/calcom/cal.com returns full_name calcom/cal.diy (the repo was renamed, GitHub redirects the old path). Every calcom/cal.com#NNNN reference in the review resolves to the same number on calcom/cal.diy, and one `--owner calcom` search covers both. The commercial Cal.com source is private, so nothing about the hosted product is verifiable and no "fixed in cal.com" claim can be made. **Re-evaluation:** Stands unchanged, high confidence. packages/platform/libraries/index.ts is untouched, so both reassign routes still await an empty async function and still answer 200 {status: success, data: {bookingUid, reassignedTo}} naming the host the booking already had, for every booking, in about 20 to 30 ms with no log line and no email. No one has reported it: nothing about the EE stubs or the reassign endpoints has appeared since the fork. The one thing re-check does not change, and which the filer MUST carry over, is the review's correction of the scenario's premise. The scenario reassignment-refuses-a-non-round-robin-booking asserts that a non-round-robin booking must be refused; the pre-fork implementation had no schedulingType gate at all and would have accepted that same call, so "the docs say non-round-robin must be refused" is not a supportable claim. The defect is the other one: on Cal.diy the endpoint can never do anything, for any booking, and reports success anyway, which contradicts the tested tree's own @ApiOperation and its ReassignBookingOutput DTO, and breaks the fork's own convention, since the four sibling stubs in the same file throw "not available in community edition".

### C9 (Cal.diy) - The documented 120 requests/min per API key is not enforced

**Filer notes:** A filer must carry the review's own uncertainty into the report: the evidence proves the documented 120/min was not enforced, it does not prove WHICH link failed open, and the report should say so and hand over the two-way header test rather than assert a cause. Do not file this as a duplicate of #29367: that issue is the Unkey limiter and the tRPC middleware, this is apps/api/v2/src/lib/throttler-guard.ts, and conflating them is what let the earlier reports be closed unmerged. Worth including in the same disclosure, because it is what makes the failure invisible to an operator: every logger.verbose in throttler-guard.ts is commented out (lines 200-208 and three earlier blocks), so a self-hoster gets no signal at all. Separately, and NOT part of the security report, the bound doc section is internally incoherent (it promises 'three authentication methods ... each of them has the following rate limits' and then lists one); that is a docs item for calcom/docs and should not be mixed into a security email. Delivery channel: the caldiy POLICY.md written for this round recommends the GitHub private advisory form at https://github.com/calcom/cal.diy/security/advisories/new AND a cc to security@cal.com, because security@cal.com is the commercial company's address and a self-host finding sent there alone may land with a team that does not own the fork; follow that rather than email alone. **Re-evaluation:** The finding stands unchanged. Both culprit files are byte-identical to the tested tree at today's head 176037d0af, which git ls-remote confirms is still origin/main, so nothing has been fixed and no rate-limiting work has merged since. The tracker is also unchanged: the only public rate-limit thread, #29367, is still open, still labelled Stale, last touched 2026-07-28, and still concerns the separate Unkey limiter in packages/lib/rateLimit.ts rather than apps/api/v2's CustomThrottlerGuard, so this is not a duplicate of it. The review's fixedAfter 'no' and its upstream claims are all still accurate. The one thing that has not moved is the review's own open question: it could not isolate whether the guard never runs on the route or whether ThrottlerStorageRedisService.increment never reports isBlocked, and nothing in the source or the tracker settles that. The live re-verify step is what settles it, exactly as the review said: capture X-RateLimit-Limit-Default / X-RateLimit-Remaining-Default on a burst. Absent headers means the guard did not run; headers counting down to 0 with no 429 means the @nestjs/throttler 6.2.1 + @nest-lab/throttler-storage-redis 1.0.0 pair is the culprit. Confidence therefore stays medium: the absence of enforcement is well evidenced (121 requests, all 200, no 429 anywhere in the run), the mechanism is not.

### C10 (Cal.diy) - A dynamic group link containing a user with allowDynamicBooking off still renders a bookable group page

**Filer notes:** File it as 'the opt-out has no effect on the public page', not as 'the link should 404'. The strongest single line of evidence for a maintainer is that the product still ships the string user_dynamic_booking_disabled in packages/i18n/locales/en/common.json referenced by no source file, next to the stale comment at getServerSideProps.ts:312-313 promising exactly that behavior. Mention that apps/web/server/lib/[user]/getServerSideProps.ts has the same gap and currently has four competing community PRs open or closed on it (#28688, #28734 open; #28789, #29273 closed) for an unrelated query-param bug, so a fix there will collide. **Re-evaluation:** The finding stands exactly as written. All three culprit files are byte-identical to the tested tree at today's head, and the decisive check is unambiguous: 'allowDynamicBooking' does not appear anywhere in apps/web/server/lib/[user]/[type]/getServerSideProps.ts on main, so the documented opt-in setting still has no effect on what the public group page renders, while the stale comment at lines 312-313 still claims the page determines 'dynamic booking not allowed' server side. Nobody has reported it: two searches covering allowDynamicBooking and dynamic-group activity since 2026-06-01 surfaced only four community PRs about query-param serialization on the sibling redirect file, none of which touches the gate. The review's cited items are all unchanged. The one caveat that survives and must travel with the filing is the review's own: the scenario asserts the literal string '404', but the product's historical intended state was a 200 page reading 'Unavailable / Some of the users in the group have currently disabled dynamic group bookings', so the correct fix would still fail the scenario as encoded. That is a scenario-encoding note, not a reason to doubt the defect; confidence stays medium only because the right REMEDY (notFound vs a booker-facing notice) is a product decision, not because the observation is in doubt.

### C11 (Cal.diy) - The booking form's phone control has no accessible name

**Filer notes:** The fix is two coordinated lines and both are needed, which the issue should say plainly: pass id={props.name} in the Components.tsx phone factory AND forward it through PhoneInput via inputProps, because react-phone-input-2 2.15.x drops a bare top-level id, so doing either one alone changes nothing. Strongest supporting evidence to include: upstream's own e2e code already works around this by selecting the control as '[name="attendeePhoneNumber"]' instead of by role and name (apps/web/playwright/lib/testUtils.ts:212), and the neighbouring email factory in the same file gets an id three lines further down. Note for the filer that C11 and C12 came out of the same failing step of the same scenario but have opposite fates: C12's prefill half is fixed on main, C11's a11y half is not, so do not let a maintainer close C11 as covered by #29740. **Re-evaluation:** The finding stands, and the single commit that landed on the culprit file since the review makes it sharper rather than weaker: PR #29740 edited the very JSX block that carries the defect, three lines above inputProps, and still did not give the tel input an id or an aria-label. So a maintainer touched this exact code on 2026-08-05 and the control is still unnamed on main. All three legs of the mechanism are current at today's head: the orphaned <Label htmlFor={field.name}> in FormBuilderField.tsx, the phone factory at Components.tsx:192-209 passing no id while its email neighbour at :220 does, and PhoneInput.tsx's inputProps at 127-132 omitting id while the component still declares an unused id?: string at line 17. Nothing is reported upstream: two searches returned zero results each. Those are weak negatives on their own, and the review's own third probe was inconclusive, so 'nobody has filed this' is a reasonable but not airtight conclusion; what would settle it is a body-text search on calcom/cal.diy for 'accessible name' or 'screen reader' plus 'booking', which I did not have budget for. Confidence stays high on the defect (the DOM mechanism is read directly from source and the review reproduced the zero-match behaviour with Playwright) and only the 'never reported' half is medium.

### C12 (Cal.diy) - The documented attendeePhoneNumber= prefill never reaches the Phone question (fixed on main by PR #29740)

**Filer notes:** Do not file. Two things the filer must not lose: (1) 'fixed' means fixed on the default branch only, so if any downstream artifact of this review says 'fixed in a release' it is wrong; nothing tags this commit. (2) The scenario prefill-the-attendee-phone-number-either-way-it-is-asked.web.1 will still fail on main, because the second, independent defect on the same step (C11, the phone control has no accessible name) is untouched by #29740; a live re-run that comes back red must not be read as '#29740 did not work'. The right live check is to read the input's VALUE via input[name="attendeePhoneNumber"] rather than by role and name, which isolates the prefill half from the a11y half. **Re-evaluation:** Confirmed fixed on main and NOT in any release. The review's 'partial' fixedAfter is exactly right and nothing has changed it: PR #29740 merged 2026-08-05 as 5e3fe3cbe6 into base main, the merge commit is on origin/main today, and the culprit line now reads country={defaultCountry} at PhoneInput.tsx:123. The one correction a filer must carry is that 'fixed' here does NOT mean 'shipped': git tag --contains 5e3fe3cbe6 is empty and the newest cal.diy tag is v6.2.0 from 2026-03-01, so every tagged cal.diy release still carries the bug, and self-hosters on a tag are unaffected by the fix. Whether hosted cal.com picked it up is not verifiable from source (the commercial tree is private), and the only public signal remains the timing of issue #29739's closure on the day the PR merged; that is suggestive, not proof, and should not be reported as 'fixed in cal.com'. No new tracker item since. The one thing to guard against is scope creep in the other direction: this scenario's step also failed for the accessible-name reason (finding C11), which #29740 did not touch, so the scenario will still not go green on main.

### C13 (Cal.diy) - The docs' own attendee-address prefill URL leaves the address input empty

**Filer notes:** The report is strongest if it leads with the control the same run provides: on the same instance minutes apart, location={"value":"phone","optionValue":"%2B919999999999"} DID populate its option input, so the radioInput optionValue mechanism works and only the address variant fails. Include the branch (a) / branch (b) question explicitly and ask for steps 4 and 5 to be run, since a maintainer can answer it in one page load. Two things not to put in the issue: the scenario's 'first textbox address' targeting is ambiguous (in DOM order 'Email address' matches first) and should be fixed to the option input's id="optionField", and the seeded organizer addresses in this fixture render as duplicate 'In Person (Organizer Address)' options, which would independently break the later steps for a seeding reason rather than a product one. **Re-evaluation:** The finding stands unchanged. Every file on the prefill path is byte-identical to the tested tree at today's head and all cited line numbers are current, so the doc's own worked example (location={"value":"attendeeInPerson","optionValue":"Delhi"}) still lands an empty address input. No new tracker item: #17394 remains the only related report and it is closed and stale, which strengthens rather than weakens the case, since it records the documented shape being the accepted working answer in late 2024. What has NOT improved since the review is its own open question, and this must travel with the filing: the review could not separate branch (a), the whole location response being dropped before bookingForm.reset so the Components.tsx:411-418 effect recreates {options[0].value, optionValue: ''}, from branch (b), the value surviving and only optionValue being lost. Source re-reading cannot settle that; only a live load with the parsed responses observed, or running steps 4 and 5 of the scenario (location={"value":"NYC"} and {"value":"link"}, both non-first options), can. If branch (a) holds, the finding's scope is wider than the address field: the whole location parameter would be ignored, and step 1's 'the option is pre-selected' pass would be a coincidence of attendeeInPerson being options[0]. Verdict and confidence are unchanged (the address is demonstrably empty in the screenshot, the video and the value read), but the filed issue should state the two branches rather than pick one.

### C14 (Cal.diy) - Default GET /v2/slots format documented as an array of strings; it is objects

**Filer notes:** File C14 and C15 as ONE issue (or one PR): both wrong strings live in the same @DocsResponse decorator on the same method of the same file, lines 202-229 and 205 respectively, and the real fix for both is the same structural one, point the DocsResponse schema at the existing Slot_2024_09_04 / SeatedSlot_2024_09_04 / SeatedRangeSlot_2024_09_04 DTOs instead of hand-writing it, which is what let the schema drift from the code in the first place. Splitting them invites two half-fixes. Two supporting facts worth quoting: the decorator's own inline example at lines 216-228 already contradicts its own schema, and the repo's e2e fixtures (apps/api/v2/.../e2e/expected-slots.ts) pin the object shape, so no behavior change is being requested. Note also that scenario step 5 of list-the-available-slots-of-an-event-type.api.1 encodes the same false premise (format=time entries must have no start property) and must be regenerated together with the doc fix. **Re-evaluation:** The finding stands unchanged, and it is now confirmed live rather than only in the snapshot: the served cal.com docs page still prints both the wrong sentence and the items: string schema today, and the in-repo source of that text is byte-identical to the tested tree. Nobody has filed it. I must be honest about the strength of that last claim: both of my searches returned zero results, and the review already observed that multi-term zero-result queries in this org are weak evidence, since a query whose terms match PR #21196's own title also returned nothing. So 'not reported' is a reasonable inference from four zero-result queries across two agents, not a proven negative; a body-text search on calcom/cal.diy for the literal 'slots as string' would settle it. Nothing has changed the verdict (doc-bug, high confidence) or the scope: the shipped behavior is correct and pinned by the repo's own e2e fixtures, the documentation is the wrong artifact, and the fault is in the product repo, not in a docs repo.

### C15 (Cal.diy) - Seated slots documented under attendeesCount; the emitter sends seatsBooked/seatsRemaining/seatsTotal

**Filer notes:** File jointly with C14: same file, same @DocsResponse decorator, same method, and the same structural fix (reference the Slot_2024_09_04 / SeatedSlot_2024_09_04 / SeatedRangeSlot_2024_09_04 DTOs instead of hand-writing the response schema) closes both. The suggested replacement sentence is 'For seated slots each object also has seatsBooked, seatsRemaining and seatsTotal, and bookingUid when the slot already has a booking'. Include the two supporting facts: the emitter is slots-output.service.ts:91-104 and :188-203, and the product's own e2e suite asserts seatsBooked/seatsRemaining/seatsTotal at user-event-type-slots.controller.e2e-spec.ts:900-952, so no behavior change is being asked for. Also worth saying that attendeesCount matches no version of the endpoint ever: the predecessor 2024-04-15 emitted 'attendees', not 'attendeesCount'. **Re-evaluation:** The finding stands unchanged and is confirmed live: the sentence promising attendeesCount is still on the served cal.com reference page today, and its in-repo source is byte-identical to the tested tree at line 205. Nobody has reported it; the single org-wide hit for the token is the same unmerged 2025 refactor the review already dismissed. Nothing changes the verdict (doc-bug, high confidence), the scope or the fault location: the emitter and the typed DTOs are correct and pinned by the repo's own e2e assertions, the hand-written decorator prose is the wrong artifact, and it lives in the product repo. The one refinement the review already flagged and that survives re-verification is that the same sentence is wrong twice over: bookingUid is NOT on every seated slot, only on slots that already have a booking, because slots-output.service.ts returns bookingUid: undefined and JSON.stringify drops the key. That is worth carrying into the filing so a maintainer does not fix only the seat names.

### C16 (Cal.diy) - The help centre's worked prefill example uses the deleted /{user}/book route

**Filer notes:** Given calcom/help's practice, a one-line PR against bookings/prefill-fields.mdx will land far faster than an issue; treat 'docs repo issue' as the fallback if a PR is out of scope for this filing round, and say in either that the identical line exists in the calcom/docs mirror (pages/core-features/bookings/prefill-fields.mdoc line 19) so both need the edit. The fix is not a path swap: type=12345 and duration=30 are old-booker parameters that do nothing on the current booker, so propose https://cal.com/johndoe/30min?email=johndoe%40example.com&notes=Test+Notes and point at the sibling page calcom/docs legacy/pages/developer/pre-fill.mdx, which already uses the correct modern shape. Worth including the hazard #6612 itself raised: because 'book' is now an ordinary event-type slug, a host who owns an event type slugged 'book' turns the stale URL into a plausible wrong page with the prefill applied rather than an honest 404. **Re-evaluation:** The finding stands and is now confirmed on the live help centre, not only in the repo. The product side is unchanged and correct: no [user]/book route exists at today's head, so the printed address still 404s, deliberately, per #6612. The doc side is unchanged in all three places: calcom/help bookings/prefill-fields.mdx line 20 on main (path untouched since 2025-11-17), the calcom/docs mirror line 19 (untouched since 2024-02-15), and the served page at https://cal.com/help/bookings/prefill-fields, which returned 200 today and still prints the URL. No tracker item proposes fixing it. Nothing changes the verdict (doc-bug, high confidence). One correction of emphasis for the filer rather than of fact: the review is right that fixing the path alone is not enough, because type=12345 and duration=30 are inert old-booker parameters on the current booker, so a correct example is closer to https://cal.com/johndoe/30min?email=johndoe%40example.com&notes=Test+Notes.

