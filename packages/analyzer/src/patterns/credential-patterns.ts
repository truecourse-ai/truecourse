/** Authentication header names, not guesses about which vendor an env name belongs to. */
export const CREDENTIAL_HEADER = /^(?:authorization|proxy-authorization|cookie|(?:x-)?(?:api[-_]?key|auth[-_]?token|access[-_]?token))$/i
