{"rule":"regex-empty-repetition","shape_sig":"91d288bb2249","class":"FP","why":"(?:[-_][a-z0-9]+)* — the non-capturing group is non-empty (requires [-_] then one+ alnum chars). The * quantifier makes the whole separator-segment optional, which is the intended design for slug patterns. No empty repetition.","skip_hint":"Rule should not flag * on a non-empty group; empty-repetition only applies when the repeated group can match empty string"}
