# Guard generation coverage

Task 1 (rollout verification) is complete, as confirmed by the user.

- Task 2 — Executable supporting browser controls. STATUS: COMPLETE. Keep user tasks and represent cancel branches, pagination and other supporting interactions as executable interfaces with source-grounded locators and state/place contracts.
- Task 3 — Honest matching gaps. STATUS: COMPLETE. Retain matched portions and report missing catalog actions separately from unavailable verification capabilities. An incomplete catalog cannot establish absent application behavior.
- Task 4 — Verification requirements. STATUS: COMPLETE. Carry explicit verification methods from claim extraction through matching and fidelity review. Separate observable contracts from internal guarantees without discarding either.
- Task 5 — Partial scenario persistence. STATUS: COMPLETE. Accept independently verified milestone subsets, preserve accepted scenarios when other work blocks, and leave uncovered obligations visible and retryable. Preserve chain requirements and never count unreviewed or stale proof as complete.

Validation completed: 1,160 tests passed across 76 files covering shared schemas, interface authoring, matching, extraction/synthesis, worker acceptance and persistence, incremental generation, dashboard coverage reads, and real Chromium cancel/pagination actions. One existing recipe-fixture test was skipped. Shared and guard-generator TypeScript builds and the core typecheck passed; git diff whitespace checks passed.

Existing expense-tracker reports were not rewritten and no production generation was triggered. Regenerate the interface catalog and tests to apply these changes. The new prompt fingerprints invalidate the corresponding caches. No dev servers were started, stopped or restarted.


## Case-level verification and status correction (September 9)

- Step 1 — Separate web/API behavior, configuration and implementation promises. STATUS: COMPLETE. New claims declare a scope, observation method, explicit acceptance cases and preparation/failure conditions. Browser behavior does not require database inspection.
- Step 2 — Validate obligation boundaries before composition. STATUS: COMPLETE. Both extraction checks and final outcomes reject invalid case metadata and mixed observations/conditions; area and epic composition enforce the same boundaries. Prompt guidance preserves named boundary cases.
- Step 3 — Match actual observation capabilities. STATUS: COMPLETE. Required observations are checked against the executor registry. Structured capability, configuration and generation blockers survive persistence/read-model projection; generic or milestone-prefixed blockers no longer become invented setup tasks.
- Step 4 — Require independent case evidence. STATUS: COMPLETE. Assertions name case IDs; the fidelity reviewer cites real assertion steps for each selected case. Evidence is bound to the reviewed scenario fingerprint. Scenario titles/promises include only selected claims, and missing cases remain visible.
- Step 5 — Preserve partial work and distinguish statuses. STATUS: COMPLETE. Verified portions survive later blocked/rejected work and combine across scenarios. The Tests table and flow detail separate execution from coverage, show generation state and identify/filter system contracts.
- Step 6 — Regenerate expense-tracker and review its new live results. STATUS: USER DEFERRED. The user will do this later; no live generation or artifact rewrite is part of steps 1–5.

Validation for this increment: STATUS: COMPLETE. 2,631 tests passed across 192 files; one existing test was skipped. This includes extraction/cache replay, area/epic boundaries, per-case review and persistence, actual API lifecycle assertion shapes, server execution/coverage projection, edited-scenario review invalidation and the four-passing-scenarios/two-complete-flows UI regression. Shared, guard-generator and core TypeScript builds and the dashboard-client typecheck passed. The UI mechanical detector and git diff whitespace check reported no findings. No dev servers were managed.

## Hide internal test resources from Dependencies (September 9)

- Dashboard visibility. STATUS: COMPLETE. The shared dependency hook exposes only supplied resources after reads, saves and refreshes. Creatable and seedable resources remain in the engine catalog. Lists, counts and detail links use the same filtered view; stale internal-resource tabs are hidden.
- Dependency presentation. STATUS: COMPLETE. Remove the Class column and filter. Prepared repositories with only internal resources show "No dependencies to configure." Repositories awaiting detection still prompt for setup.
- Validation. STATUS: COMPLETE. 58 tests passed across the dependency regressions, preview route smoke tests and preview fixtures. Coverage includes hidden resource detail links, supplied service links, registration saves, refreshes and empty states. Dashboard-client typecheck, UI mechanical detector and git diff whitespace checks passed. No pipeline rerun or dev-server changes were needed.


## Expense-tracker generation blockers and compact labels (September 9)

- Database investigation. STATUS: COMPLETE. The 10:54 generation report contains no committed scenario for either requested flow. Add stopped at invalid matcher references after correction. Edit's probes found an empty list and a missing seeded expense; its untagged diagnostic probe was rejected by our coverage gate. Matching also incorrectly called a browser refresh an unmapped action. The stored trace does not expose the exact invalid Add reply or establish why the seeded expense was absent.
- Browser generation context. STATUS: COMPLETE. Brief authors with other mapped browser setup actions and their places, scoped to the served app where route ownership is known. Create owned records before edit/delete rather than changing shared fixtures. Include the setup catalog in worker/generation cache inputs and the estimate. Teach native navigation to the same path for full-page refresh.
- Probe and matcher validation. STATUS: COMPLETE. Permit untagged diagnostic executions while preserving strict submission evidence requirements and validation of any selected cases. Give the matcher's corrective turn exact gap conflicts, duplicate gaps and unknown references, and retain those details if it still fails.
- Compact test status. STATUS: COMPLETE. Restore the original single status label and filters in Tests and flow lists. Keep detailed execution/coverage evidence collapsed below the flow title and goal.
- Validation. STATUS: COMPLETE. 945 tests passed across 69 files, with one existing test skipped. This covers generator authoring/persistence, real Chromium actions, setup-context/cache invalidation, estimates, strict submission proof and compact status filters. The intentional cache-hash pin was updated and its regression rerun passed. Shared, guard-generator and core builds and dashboard-client typechecking passed. The UI detector and whitespace checks passed.
- Live artifacts. STATUS: UNCHANGED. No stored scenarios, setup recipes or live database rows were edited and no pipeline was rerun. Existing flow statuses require another generation to reflect the code changes. No dev servers were managed.

## Fix browser crash from review hashing (September 9)

- Module boundary. STATUS: COMPLETE. Move `scenarioReviewFingerprint` from the browser-imported proof module to the server-only `@truecourse/shared/guard-proof-node` entry point. Update generation and dashboard read consumers, preserving the existing hash algorithm and stored review fingerprints.
- Regression coverage. STATUS: COMPLETE. A browser-platform bundle test reproduced the original `node:crypto` resolution failure before the fix and passes afterward. All 99 targeted tests passed across proof/case validation, generation persistence, server flow reads and dashboard routes/dependencies. Shared, guard-generator, core and dashboard-client builds passed. Vite still reports existing externalization warnings from other shared modules and large-chunk warnings.
- Live verification. STATUS: COMPLETE. Reloaded the user's Chrome dashboard, confirmed the crypto exception cleared and the dashboard rendered, then opened expense tracker Dependencies and confirmed zero rows with "No dependencies to configure." No dev servers were started, stopped or restarted.

## Complete required guard checks before ending generation (September 9)

- Investigation and implementation plan. STATUS: COMPLETE. Read-only Postgres inspection confirmed the expense-tracker worker removed post-save and cancellation checks after failures/rejections, accepted five of ten cases, and explicitly returned settled. Detailed implementation and validation: [Plan 001](plans/001-complete-guard-generation.md).
- Required-case accounting. STATUS: COMPLETE. Shared progress calculation retains exact outstanding obligations after omissions and rejections. Current reviewed priors and the latest accepted replacements supply proof; stale bindings, edited YAML and incomplete evidence do not. Rejection feedback survives smaller accepted submissions. Authored coverage remains separate from passing execution.
- Browser assertion capabilities. STATUS: COMPLETE. Added retrying hidden, visible cardinality and current input-value assertions, including browser-local date defaults. Chromium regressions cover ambiguity, hidden/detached targets, DOM properties, timezone boundaries and broken expense behavior.
- Bounded completion enforcement. STATUS: COMPLETE. Optional session outcome validation refuses premature settled responses and resumes the same cumulative budget. Both provider adapters exercise continuation. Partial accepted artifacts survive blockers, retirement and exhaustion; final persistence reconciles remaining requirements again.
- Cache evidence and compatibility. STATUS: COMPLETE. Versioned worker completion caches carry review policy, scenario fingerprints and case evidence. Completion requires current assigned coverage before runtime replay. Missing/old/partial review evidence cannot skip authoring; the review policy also participates in generation hashes.
- Case fidelity and starting state. STATUS: COMPLETE. Extraction/review guidance separates primary ordering from tie-breaks, requires independent date/default checks, controlled empty state and totals, and every promised post-save transition. Explicit expected failures receive case review and preserve unreviewed status when the reviewer is unavailable.
- Incident regression and rollout validation. STATUS: COMPLETE. Synthetic replay and real-browser healthy/broken fixtures pass. Full suite: 694 files passed, 13,298 tests passed, 32 existing skips; all 34 new browser tests executed. Core and dependency builds, core/dashboard typechecks and diff whitespace validation pass. README documents assertions, retries and cache semantics.
- Live regeneration. STATUS: ANALYZED. The user reran generation cd2ec0ff-d11c-4c09-a553-5f3b686e2ec3. Read-only inspection confirmed new review policy and partial accounting, but only 6/17 flows completed; the following run passed 9/10 scenarios. Remaining engine defects are tracked below. No dev servers were managed or DB rows edited by this analysis.


## Repair live generation failures after Plan 001 (September 9)

- Rerun investigation and follow-up planning. STATUS: COMPLETE. Read-only DB evidence identified shared ledger interference, malformed faithful-review citations repeatedly treated as semantic failures, milestone-level matching suppression, and retirement before independent repairs. Plans are self-contained in [the plan index](plans/README.md).
- Reviewer evidence repair. STATUS: COMPLETE. [Plan 002](plans/002-repair-fidelity-evidence.md): structured step diagnostics, bounded validation/repair inside the reviewer, validated cache hits, and separation of metadata failure from weak-test findings.
- Controlled scenario data. STATUS: COMPLETE. [Plan 003](plans/003-isolate-scenario-data.md): verified preparation profiles, per-execution data scope, consistent environment/seed/credentials, bundle/fingerprint support, and real concurrent regression fixtures. Preserve shared infrastructure lifecycle ownership.
- Individual-case matching. STATUS: COMPLETE. [Plan 004](plans/004-match-individual-verification-cases.md): split UI/protocol/metadata contracts, plan/gap exact case IDs, preserve independent browser cases, and repair catalog gaps through the existing ownership boundary.
- Repair before retirement and combined validation. STATUS: COMPLETE. [Plan 005](plans/005-repair-before-retirement-and-verify-rerun.md): accurate per-case remainder reasons, bounded repair of stale/recoverable outcomes, independent accepted portions, and full regression/rollout gates.
- Additional executor capabilities. STATUS: DEFERRED. Request interception and authored timezone control remain explicitly unsupported. These follow-up plans do not conceal those gaps or promise all flows will pass.

- Follow-up implementation validation. STATUS: COMPLETE. Plans 002–005 now cover typed evidence repair, verified private preparations with targeted Setup, per-case matching/retained-proof checks, and bounded current-state retirement. Final suite: 699 files passed, 13,355 tests passed, 32 existing skips. Real HTTP/Chromium isolation, count/total/order/page, cancellation, cleanup and bundle regressions passed. Core/dependency builds, core/dashboard typechecks and diff whitespace validation passed.
- Follow-up hosted rollout. STATUS: NOT RUN. Deploy the engine/runner, refresh preparations with `truecourse guard setup --only-preparations --refresh`, then regenerate. Keep existing DB history. Missing catalog actions remain precise interface-authoring tasks; unsupported executor capabilities remain explicit gaps. No hosted generation, deployment or DB mutation was performed during implementation.

## Expense-tracker live validation after Plans 002–005 (September 9)

- User-run pipeline inspection. STATUS: COMPLETE. Read-only local Postgres inspection of generation `39d49154-ed97-4840-90d3-12ea13ecfff4` and execution `2026-09-09T15-17-55Z_e0f41b70` found 12/26 flows complete, 14 saved scenarios, and 12 passing/2 failing executions. The prior recorded baseline was 6/17 flows and 9/10 passing scenarios; changed flow boundaries prevent a like-for-like percentage comparison. Full findings: [live generation analysis](docs/findings/expense-tracker-guard-2026-09-09.md).
- Improvement evidence. STATUS: VERIFIED. Add 8/8, Edit 4/4, and Delete 4/4 cases are reviewed and passing. Reviewer citation repair succeeded within one live review session. Accepted API portions survived a later malformed worker outcome. All 14 saved scenarios have valid current review fingerprints and case-evidence references. The manifest records 61/118 reviewed cases, including five cases in the two failing scenarios.
- Remaining defects. STATUS: OPEN. The generated `sqlite-empty` preparation inserts a $23.47 fixture and verifies one row as empty, causing both failures; expected-red review incorrectly certifies this as application drift. Filtering retires after correcting remaining-case prose without another repair submission, and aggregate rejection reasons contaminate other case gaps. API worker schema-invalid final output bypasses outcome correction. A matcher timeout leaves nine cases uncovered; timezone invariance reaches authoring despite unavailable timezone control.
- Scope. STATUS: COMPLETE. Updated investigation documentation only. No database rows, stored scenarios, application code, or dev servers were changed. No generation or execution rerun was triggered by this investigation. The earlier NOT RUN rollout entry records implementation-time status; this entry records the user's subsequent local pipeline.

## Repair the September 9 live preparation and worker failures

- Empty baseline contract. STATUS: COMPLETE. Preparation drafts now declare unfiltered API baseline checks with independently known global counts and totals. The runner performs the checks against both private allocations before the generated isolation verifier, then checks the primary again afterward. Empty profiles require zero business counts/totals. A verifier marker cannot override observed data, and a contaminated profile produces a preparation error before any scenario assertion or expected-red certification. Authors receive the known baseline values.
- Legacy preparation migration. STATUS: COMPLETE. Recipes remain readable, but profiles without baseline checks are unavailable for assignment, prior-proof validation and execution. Setup reauthors old profiles even when their old fingerprint was settled; the estimate follows the same rule. Main seeds and historical artifacts remain unchanged.
- Executable repair before retirement. STATUS: COMPLETE. Corrections record the candidate behaviors already attempted for each outstanding case. A revised terminal explanation or identical resubmission cannot satisfy the correction. Changed executable submissions are required; continuing refusal exhausts the original session budget. Accepted portions remain saved. Worker prompt fingerprints intentionally changed.
- Malformed terminal outcome correction. STATUS: COMPLETE. Guard workers opt into at most two schema corrections under the existing cumulative budget, before task-state validation. The regression reproduces `blocked` plus forbidden `additionalScenarios`, retains accepted portions and completes the remaining cases after correction. Other session types retain their existing malformed-outcome policy.
- Validation. STATUS: COMPLETE. Across the guard/session/provider selection and targeted corrections, 2,453 tests passed across 176 files, with one existing skip. This includes the intentional worker fingerprint update, incident replay, and legacy migration/estimate regression. The final affected-file reruns passed 64 and 70 tests respectively. Core and dependency builds and git diff whitespace checks passed. HTTP/Chromium preparation, isolation, cancellation and completion fixtures executed. README documents baseline checks, compatibility, executable repair and bounded schema correction. No live pipeline, database mutation or dev-server management occurred.
- Remaining follow-up. STATUS: OPEN. Aggregate fidelity-rejection attribution still needs exact per-case findings; matcher timeout recovery and early timezone-capability classification remain outside this fix. New live-model results require refreshed preparations followed by regeneration and execution. The historical report remains evidence of the previous run.


## External credential detection (September 9)

- Detect JS/TS environment reads reaching native fetch authentication headers and associate them with their resolved service, retaining file/line/header evidence and no values. STATUS: COMPLETE. Covers local aliases, URL construction, common auth headers, shadowing and ambiguous-source exclusions.
- Carry optional credential metadata through shared analysis snapshots into required secret recipe and catalog fields. STATUS: COMPLETE. Old snapshots remain readable.
- Enrich existing declarations and supplied env registrations without replacing custom settings, classifications, or saved secrets. STATUS: COMPLETE. Runs even when catalog classification is settled; detection fingerprints include credential names.
- Regression verification for detection, migration, secret field projection, local storage/redaction and actual runner injection. STATUS: COMPLETE. 289 tests passed across 15 files; the final focused extractor run passed 26 tests after adding two more shadowing cases. Shared, analyzer, guard-generator and core TypeScript builds passed. An authenticated localhost provider verified that a missing key stops the dependent scenario while an independent scenario passes, supplying the key lets both pass, and an invalid key fails. Secret values stay out of committed declarations and reported evidence. No dev servers were managed.
- Verify the actual expense-tracker source. STATUS: COMPLETE. Its CurrencyBeacon Authorization header now produces CURRENCYBEACON_API_KEY evidence in addition to CURRENCYBEACON_BASE_URL.
- Live CurrencyBeacon account verification in expense-tracker. STATUS: PENDING. Requires the user's real key and a subsequent setup/generate/run; no live account or stored application artifacts were modified.
